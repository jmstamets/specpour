G1 (age-gate frontend, HIGH) — accept, and place it Foundational, not US1. The gate must precede all guest browsing, so it belongs before the first guest-facing story, not inside one. Suggested task wording: "Flutter compliance/age-gate feature: DOB-entry UI (form entry, not checkbox); fetch jurisdiction threshold from /compliance/age-gate; strictest-rule fallback when the jurisdiction call fails or the device is offline; persist affirmed flag locally only." Two acceptance tests that make the privacy requirement real rather than aspirational: (1) intercept all network traffic during gate flow and assert the DOB value appears in no request payload, (2) assert nothing DOB-derived exists outside local storage. The offline case matters given your offline-first constitution — no connectivity means no jurisdiction lookup, so strictest-rule must be the local default, and that's worth an explicit test too.
G2 (notification adapters, MEDIUM) — split the two dependents. The email adapter is non-optional: account recovery (US2) is a V1 hard requirement and can't pass end-to-end against a stub. Add a real adapter task before US2 — dev SMTP (e.g., a local mail-catcher) for local/CI, production provider config-selected per the constitution's adapter rule; the port contract test you already have makes this cheap. For push, I recommend the defer branch: prep-expiry alerts (US10) are adequately served by in-app inbox (the constitution's default, always-on channel) plus email opt-in, and standing up FCM/APNs is real cost that Phase 2's consumer engagement will actually justify. So: amend FR-040a/US10-scenario-1 to "in-app notification, email if opted in; push deferred to Phase 2," and move the push-adapter task to the Phase 2 backlog. Long-term-economics principle applied in the deferring direction, which is exactly what it's for.
G3 (recognition eval, MEDIUM) — accept, and note it's constitutionally implied anyway. Your constitution already ties prompt versions to evaluation results — SC-008's harness IS that evaluation infrastructure for the vision feature, so this task was always owed. Add: curated labeled bottle-photo set (include the hard cases: glare, partial labels, non-English labels, craft bottles absent from training data), an accuracy harness that runs per prompt-version/provider pair, and results recorded against the prompt registry. And accept the T140 rescope: list which SCs are automated vs. manually verified — SC-008 (dataset-driven, semi-automated) and SC-001 (3-minute timing, manual UX protocol) can't honestly sit under "automated verification."
I1 (shopping↔prep dependency, MEDIUM) — fix the note, don't reorder. Correct the US9 dependency entry to include US10 for the prep-sourced path, and tag those acceptance tests to run at the Phase-13 checkpoint — exactly the pattern the menu-sourced path already uses for its US11 deferral. Reordering phases to avoid a partially-deferred story would trade a documentation fix for a schedule disruption; not worth it.
I2 (facet timing, MEDIUM) — accept as written. Scope T037/T038 to content facets (family, category, tags, flavor profile, glass, equipment, allergen, ABV range), add facet-completion sub-tasks to US4 (makeable-from-inventory facet) and US6 (rating facet), and note the deferral in US1's Independent Test. This is the correct ATDD shape: the scenario stays whole, its acceptance tests are staged to the checkpoints where their backing projections exist.
U1 (ai/search persistence, MEDIUM) — decide it this way: add an ai schema owned by the AI-assist module (prompt-version metadata, provider bindings, eval results — the registry is relational data with real referential needs, and burying eval results in config files fights the constitution's prompt-versioning requirement; a sensible hybrid keeps prompt template text in the repo for code review while the registry/eval records live in the schema). Do not add a search schema: search is a port, not a module that owns domain data — tsvector/trigram columns live in each owning module's schema, maintained via events, which is precisely consistent with module-owns-schema; a future dedicated engine brings its own external index via the adapter. Update T013 (18 schemas) and data-model.md, and capture the whole decision as an ADR since it interprets Principle 3 — this is exactly what ADRs are for.
A1 (SC-012 undefined, MEDIUM) — here's a concrete definition to ratify: 10,000 concurrent sessions, sustained 30 minutes, under a stated workload mix of roughly 80% reads (recipe/glossary/browse), 15% search, 5% writes (ratings, library edits, batch calcs); pass = p95 latency ≤ 300ms for reads, ≤ 500ms for search (matching your existing search p95), ≤ 600ms for writes; error rate < 0.1%; no degradation trend across the sustain window; AI-assisted endpoints excluded (provider-latency-bound, measured separately with their graceful-degradation behavior asserted instead). Adjust the numbers to taste, but T137 shouldn't be written until some version of this is in SC-012 — a load test without pass criteria is theater.
One meta-observation: G2's defer decision and A1's thresholds are the only two that change requirements rather than tasks — make sure those two amendments land in the generated spec.md itself (FR-040a and SC-012), not just in tasks, or the next audit pass will correctly flag spec/tasks drift in the opposite direction.
