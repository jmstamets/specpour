namespace SpecPour.Modules.Catalog.Domain;

/// <summary>data-model.md ConceptVariantLink.state (FR-021): user attachments are curator-moderated.</summary>
public enum ConceptVariantState
{
    Proposed,
    Approved,
    Rejected,
}
