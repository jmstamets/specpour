namespace SpecPour.Modules.Media.Domain;

/// <summary>data-model.md MediaAttachment.kind — photo now, video later without remodeling (FR-023).</summary>
public enum MediaKind
{
    Photo,
    Video,
}
