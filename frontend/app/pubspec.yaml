name: specpour_app
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  go_router: ^17.3.0
  flutter_riverpod: ^3.3.2

  # Generated Dart client (T008) — never hand-edited. Path dependency since it's a
  # sibling package in this monorepo, not published to pub.dev.
  api_client:
    path: ../packages/api_client
  drift: ^2.34.1
  drift_flutter: ^0.3.1
  shared_preferences: ^2.5.5

  # ADR-0005 (T177): platform-appropriate refresh-token persistence — Keychain/
  # Keystore-backed on native, chosen at runtime alongside shared_preferences
  # (web) in core/auth/token_store.dart. Not a conditional-import split like
  # web_authorize.dart — both packages compile cleanly on every target, so a
  # runtime kIsWeb branch is enough (see token_store.dart for why the web path
  # deliberately does NOT use this package's own web fallback, which is an
  # unencrypted localStorage shim that would misrepresent its security
  # properties).
  flutter_secure_storage: ^9.2.4

  # T047/ADR-0003: the identity sign-in flow needs direct Dio/cookie-jar access
  # (register/login establish a cookie; the client then drives the
  # authorization-code+PKCE exchange itself against /connect/authorize +
  # /connect/token, reusing that cookie) — dio was previously dev-only (test
  # fake-interceptor doubles); now needed in lib/ too.
  dio: ^5.10.0
  cookie_jar: ^4.0.8
  dio_cookie_manager: ^3.2.0
  crypto: ^3.0.6
  # T-walkthrough#1 (2026-07-15): web-only fetch to read the /connect/authorize
  # redirect's final URL (Dio can't on web). See core/auth/web_authorize.dart.
  web: ^1.1.0

  # T049: social sign-in is a real browser-redirect OAuth handshake, not a
  # Dio call — the app must navigate the whole tab to the provider's own
  # consent screen (webOnlyWindowName: '_self' on web; a system browser tab
  # on native, never a WebView, which providers discourage/block for sign-in).
  url_launcher: ^6.3.2

  # T187: renders the MFA enrollment otpauth:// URI as a scannable QR code
  # (CustomPainter-based, works on every target incl. web/CanvasKit). The
  # backend already returns the otpauth URI; this just makes it scannable
  # instead of forcing manual secret entry.
  qr_flutter: ^4.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  drift_dev: ^2.34.0
  build_runner: ^2.15.1
flutter:

  # Regenerates lib/core/l10n/gen/ from lib/core/l10n/arb/ (see l10n.yaml) on every
  # build, per constitution Principle VII (i18n from first commit).
  generate: true

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
