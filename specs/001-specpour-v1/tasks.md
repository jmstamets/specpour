---

description: "Task list for SpecPour V1 implementation"
---

# Tasks: SpecPour V1

**Input**: Design documents from `/specs/001-specpour-v1/`

**Prerequisites**: plan.md, spec.md, research.md, data-model.md, contracts/api-v1-surface.md, quickstart.md

**Tests**: MANDATORY per Constitution v1.4.0 Principle I (ATDD). Every user story begins
with Given/When/Then acceptance tests that MUST fail before implementation (red-green).
CI merge gates: acceptance + unit + integration + contract suites green.

**Organization**: Tasks are grouped by user story (US1–US16 from spec.md) so each story
is independently implementable and testable. Backend paths are `backend/…`, client paths
`frontend/…`, per plan.md's structure. Each module follows
`backend/src/Modules/<Module>/{Domain,Application,Infrastructure,Contracts}/`.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependencies)
- **[Story]**: Which user story this task belongs to (US1–US16)

> **Note (post-analysis amendment)**: Tasks T144–T150 were added by the /speckit-analyze
> remediation pass, and T151 by the 2026-07-11 post-implementation gap review (inbox
> read / channel-preference endpoints present in contracts/api-v1-surface.md but not
> previously scheduled). Task IDs are stable identifiers; **phase placement defines
> execution order**, so amended tasks execute within their listed phase, not by ID
> sequence.

> **Standing convention (2026-07-14, /speckit-analyze U1)**: `[X]` means the task's
> deliverable is actually done — full stop, no exceptions. A scheduling or scope
> decision (e.g. "this work moves to Phase N, tracked there") is recorded in the
> task's own prose/sub-bullet, never by checking the box, so a naive `[X]` count
> always reflects genuine completion. (T152 was previously an exception to this and
> has been restyled to comply.)

---

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: Repo, solution, toolchain, and pipeline scaffolding

- [X] T001 Initialize git repository, .gitignore, .editorconfig, and root README.md at repo root
- [X] T002 Create .NET solution and project skeletons per plan structure in backend/src/{Api,BuildingBlocks,Modules/*,Tools/{MigrationRunner,Seeder}} and backend/tests/{Acceptance,Contract,Integration,Unit}
- [X] T003 [P] Create docker-compose.yml at repo root: postgres:17-postgis, minio, otel-collector, api service
- [X] T004 [P] Configure C# static analysis and formatting (Directory.Build.props, .editorconfig analyzers, treat-warnings-as-errors) in backend/
- [X] T005 [P] Scaffold Flutter app with feature-first layout in frontend/app/ (lib/{features,core,data}, test/, integration_test/) and enable ARB i18n workflow in frontend/app/lib/core/l10n/
- [X] T006 [P] Configure Dart analysis (analysis_options.yaml) and CI-enforced `dart format` in frontend/app/
- [X] T007 Author OpenAPI 3.1 root document with global conventions (problem+json, cursor pagination, security schemes, no-raw-DOB invariant note) in backend/contracts/openapi/openapi.yaml
- [X] T008 Create Dart client generation pipeline (openapi-generator dart-dio, CI script, hand-edit prohibition check) generating frontend/packages/api_client/ via scripts/generate-client.sh
- [X] T009 Create CI pipeline definition (build, analyzers, all four backend test suites, flutter test suites, client regen drift check, dependency & container scanning) in .github/workflows/ci.yml

**Checkpoint**: Solution builds, compose stack starts, empty test suites run in CI

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: Cross-cutting infrastructure every story depends on — no story work before this completes

- [X] T010 Implement BuildingBlocks: module composition interface (IModule), UUIDv7 generator, problem+json helpers, domain-event envelope in backend/src/BuildingBlocks/
- [X] T011 Implement outbox-backed domain-event dispatcher (outbox table pattern, transactional write, background dispatcher) in backend/src/BuildingBlocks/Events/
- [X] T012 Implement MigrationRunner tool (per-module forward-only EF Core migrations, ordered apply) in backend/src/Tools/MigrationRunner/
- [X] T013 Create base migration: 18 module schemas (identity, authz, catalog, ingredients, equipment, glossary, community, inventory, measurements, shopping, prep, collections, tastinglog, media, notifications, compliance, venues, ai), PostGIS extension, outbox and sync_change tables in backend/src/Tools/MigrationRunner/Migrations/ — Search owns no schema: tsvector/trigram columns live in owning-module schemas, maintained via events (ADR per T141)
- [X] T014 Compose Api host: module registration, middleware order (correlation ID, auth, anonymous rate limiter, problem+json), /health/live and /health/ready in backend/src/Api/Program.cs
- [X] T015 [P] Wire OpenTelemetry (traces/metrics/logs) + Serilog structured logging with correlation-ID propagation and sensitive-field scrubbing policy (dateOfBirth-shaped fields) in backend/src/Api/Observability/
- [X] T016 Implement identity module core: User entity with application-layer AES-GCM encrypted date_of_birth column (R6a), ASP.NET Core Identity stores, module schema DbContext in backend/src/Modules/Identity/
- [X] T017 Implement OpenIddict token issuance (authorization code + refresh) and auth middleware integration in backend/src/Modules/Identity/Infrastructure/OpenIddict/
- [X] T018 Implement authorization module: Tier + CapabilityGrant + PlatformRole + RoleGrant entities, capability policy layer, guest pseudo-tier floor, GET /api/v1/me/entitlements in backend/src/Modules/Authorization/
- [X] T019 Implement append-only audit log (AuditLogEntry, single audit port IAuditWriter, no UPDATE/DELETE grants in migration) in backend/src/Modules/Authorization/Audit/
- [X] T020 [P] Implement compliance module: SurfaceGateConfig + JurisdictionRule entities, IGeoIpPort + adapter, GET /api/v1/compliance/age-gate in backend/src/Modules/Compliance/
- [X] T021 [P] Implement media module: IObjectStoragePort + S3-compatible adapter (MinIO), MediaAttachment entity, pre-signed upload flow in backend/src/Modules/Media/
- [X] T022 [P] Implement search module: ISearchPort contract + PostgreSQL FTS adapter (tsvector maintenance, websearch_to_tsquery, trigram), port contract-test suite in backend/src/Modules/Search/
- [X] T023 [P] Implement notifications thin slice: InboxMessage + ChannelPreference entities, typed notification events consumer, IEmailChannelAdapter port with per-port contract-test suite (real adapter lands in T146; push delivery deferred to Phase 2 per FR-040a), GET /api/v1/inbox in backend/src/Modules/Notifications/
- [X] T024 [P] Implement measurements module: ConventionTable versioned reference data, canonical-ml conversion service (oz/ml/cl + dash/barspoon), method dilution constants in backend/src/Modules/Measurements/
- [X] T025 Implement Seeder tool: convention tables, curated launch-content ingestion pipeline (recipes/ingredients/equipment/glossary from content files), first-Super-Admin bootstrap from environment config writing inaugural audit entry in backend/src/Tools/Seeder/
- [X] T026 Set up Reqnroll + Testcontainers acceptance harness (composed host, real PostgreSQL, clock test hook) in backend/tests/Acceptance/Support/
- [X] T027 [P] Set up OpenAPI contract-test harness validating running API against backend/contracts/openapi/ in backend/tests/Contract/Support/
- [X] T028 Scaffold Flutter core: go_router navigation, Riverpod DI, WCAG AA theming + bar-mode theme tokens, entitlement-manifest gate, api_client wiring in frontend/app/lib/core/
- [X] T029 Set up Drift local store skeleton (schema v1, migrator, FTS5 enablement) and repository layer pattern in frontend/app/lib/data/
- [X] T144 [P] Flutter compliance/age-gate feature: DOB-entry UI (form entry, not checkbox); fetch jurisdiction threshold from /compliance/age-gate; strictest-rule fallback when the jurisdiction call fails or the device is offline; persist affirmed flag locally only, in frontend/app/lib/features/compliance/
- [X] T145 [P] Failing-first age-gate integration tests: (1) intercept all network traffic during the gate flow and assert the DOB value appears in no request payload, (2) assert nothing DOB-derived exists outside local storage, (3) offline/lookup-failure → strictest-rule local default, in frontend/app/integration_test/age_gate_test.dart

**Checkpoint**: Foundation ready — user story phases can begin

---

## Phase 3: User Story 1 — Discover and Follow Curated Recipes (P1) 🎯 MVP

**Goal**: Guests and users search/browse curated content and view complete recipes with derived data

**Independent Test**: Seed content; search "Mai Tai" anonymously; verify all recipe fields, ABV/standard drinks, allergen roll-up, concept-page variant navigation, content faceted filters, guest gating prompts, responsible-consumption messaging. Note: US1 scenario 5's `rating` and `makeable-from-inventory` facets are staged — their acceptance tests are tagged to run at the US6 (T149) and US4 (T148) checkpoints where the backing projections exist

- [X] T030 [P] [US1] Acceptance tests (failing first) for US1 scenarios 1–7 (scenario 8 is covered by T150's extension of this feature) in backend/tests/Acceptance/Features/US01_DiscoverRecipes.feature
- [X] T031 [P] [US1] Flutter integration test for guest browse/search/recipe view in frontend/app/integration_test/us01_discover_test.dart
- [X] T032 [P] [US1] Catalog module: Recipe, RecipeIngredientLine, RecipeRelation, ConceptPage, ConceptVariantLink, Family/Category entities + migration in backend/src/Modules/Catalog/Domain/
- [X] T033 [P] [US1] Ingredients module: Ingredient hierarchy, categories, allergen attributes, SubstitutionRule entities + migration in backend/src/Modules/Ingredients/Domain/
- [X] T034 [P] [US1] Equipment module: Equipment entity + migration in backend/src/Modules/Equipment/Domain/
- [X] T035 [P] [US1] Glossary module: GlossaryTerm (unique term, numbered definitions), Article, LinkOverride entities + migration in backend/src/Modules/Glossary/Domain/
- [X] T036 [US1] Derived-data services: ABV + standard-drinks calculator (method dilution conventions) and allergen roll-up in backend/src/Modules/Catalog/Application/DerivedData/
- [X] T037 [US1] Read endpoints + OpenAPI paths: GET /recipes (content facets: family, category, tags, flavor profile, glassware, ice, equipment, allergen-exclude, ABV range, source — rating facet lands in T149, makeable facet in T148), GET /recipes/{id}, GET /concepts, GET /concepts/{id}, GET /ingredients, GET /equipment, GET /glossary/* in backend/src/Modules/*/Endpoints/ and backend/contracts/openapi/paths/
- [X] T038 [US1] Search endpoint GET /api/v1/search over ISearchPort with content-facet filtering composition (facet set completed by T148/T149) in backend/src/Modules/Search/Endpoints/
- [X] T039 [US1] Glossary auto-linking service (first occurrence per page, longest-match, curator overrides honored, return navigation anchors) in backend/src/Modules/Glossary/Application/AutoLink/
- [X] T040 [US1] Curated seed content pack: canonical repertoire subset for test/dev (incl. Mai Tai, Daiquiri concept + variants, egg-white flip) in backend/src/Tools/Seeder/Content/
- [X] T041 [US1] Flutter discover feature: home/browse/search screens with facet UI in frontend/app/lib/features/discover/
- [X] T042 [US1] Flutter recipe detail + concept page screens (all content fields, derived data, allergen prominence, glossary links with return navigation; FR-022's per-serving cost display is staged — it lights up at the US5 checkpoint where T075/T076 provide pricing data, mirroring the T148/T149 facet pattern) in frontend/app/lib/features/discover/recipe_detail/
- [X] T043 [US1] Guest gating UX: account-gated action prompt with post-signup intent completion hook; anonymous rate limiting verified in frontend/app/lib/core/guest_gate/
- [X] T044 [US1] SEO HTML projection endpoints GET /pages/recipes/{slug} etc. for public content in backend/src/Api/Seo/
- [X] T150 [US1] Responsible-consumption package (FR-067/FR-068/FR-069, US1 scenario 8): jurisdiction-configurable message text + placement served by the compliance module (GET /compliance/messaging, GET /compliance/support-resources); persistent messaging on recipe pages, batch/scaling outputs, and app footer/about; support-resource links reachable from the messaging and settings/about; per-serving ABV/standard-drinks display designated to Principle XIII; extend US1 acceptance feature + widget coverage, in backend/src/Modules/Compliance/Application/Messaging/ and frontend/app/lib/core/responsible_use/
  - §20 four-element coverage (confirmed 2026-07-11): FR-067 (persistent message) — DONE: ResponsibleConsumptionMessage entity + GET /compliance/messaging (default fallback per surface), rendered by ResponsibleUseBanner on RecipeDetailScreen (surface=recipe) AND the new AboutScreen footer/about surface (surface=footer_about). FR-069 (support resources) — DONE: SupportResource entity + GET /compliance/support-resources, reachable from the banner's support-resources button and the About surface. FR-068 (quantified transparency) — per-serving ABV/standard-drinks is DONE (recipe detail, T042/FR-022); the BATCH total-standard-drinks + per-serving-strength half is owned by T074 (US5 batching, already names FR-068) — correctly NOT built here. FR-070 (responsible-service/consumption encyclopedia launch content) — NOT code: flagged to the launch content checklist (see the "Launch content checklist" note under the Notes section), same as the 300-term glossary and repertoire content. Batch/scaling-output message placement (FR-067) also lands with T074's batch UI, since no batch output surface exists yet in US1.
- [X] T154 [US1] Typed/grouped search results in the Discover UI (2026-07-12 human review of the Phase 3 build, reconciling amended FR-049): search results grouped in presentation by entity type (Recipes / Ingredients / Equipment / Glossary) with section headers, so the user always knows what kind of entity each result is — done immediately per John's "before it hardens" instruction, in frontend/app/lib/features/discover/
- [X] T155 [US1] Backend search reconciliation (2026-07-12 human review, amended FR-049/FR-050/FR-014a; ISearchPort projection change, T022/T038 territory; **architecture APPROVED by John 2026-07-12, recorded as ADR-0002**): (a) recipe search-document composition — all names + ingredient names at the referenced hierarchy level + garnishes + description/history — so text search for an ingredient surfaces recipes using it; because ingredient names cross the module schema boundary, the search document must be event-maintained (outbox-driven refresh when a recipe's lines or a referenced ingredient's name change), per the T013/T141 "tsvector columns live in owning-module schemas, maintained via events" ADR — the current PrimaryName+AlternateNames generated column can't reference another module's table; (b) hierarchy-aware "uses: <ingredient>" facet on GET /recipes and /search (class-level ingredient matches recipes using it or any descendant); (c) FR-014a ingredient→recipes bidirectional surface: GET /ingredients/{id}/recipes (hierarchy-aware, mirror of equipment's FR-024 link), in backend/src/Modules/{Search,Catalog,Ingredients}/. **Riders (John, 2026-07-12, in scope)**: (1) an idempotent, re-runnable BACKFILL of all existing recipe rows when the event-maintained document ships — events only cover changes going forward; (2) a refresh-path acceptance test: rename an ingredient, assert recipes using it become searchable under the new name AND stop matching the old one; the eventual-consistency window (brief post-rename staleness, bounded by outbox dispatch latency) is documented in ADR-0002 and is acceptable; (3) walkthrough smoke test: searching "rum" must surface Daiquiri, Hemingway Daiquiri, and Mai Tai under a Recipes header — none contains "rum" in its name, so this exercises the composition directly (already noted in docs/mvp-walkthrough-checklist.md)
  - **Phase 3 (US1) MVP checkpoint CLOSED 2026-07-14**: docs/mvp-walkthrough-checklist.md's human walkthrough (build rebuilt through T164, checksum-verified served-vs-disk) passed all six items — T155 search-document composition (rum search), T157 resolved labels, T158 garnish/ice labeling (confirmed renderer bug, not seed content — no launch-content QA entry needed), FR-055 allergen prominence (carried forward from John's 2026-07-12 verification, unaffected by intervening identity-module work), T156 popovers, T150 responsible-consumption banner. Next: T163.
  - **Implementation notes (2026-07-12)**: `Recipe.SearchDocumentText` (plain mapped column) replaces PrimaryName+AlternateNames as SearchVector's generated-column source (migration `AddRecipeSearchDocument`, EF's AlterColumn translated to DROP+ADD for the generated expression as expected — GIN index explicitly recreated since dropping the column cascades to drop it); `SearchDocumentComposer` (Catalog.Application, pure, unit-tested) composes PrimaryName+AlternateNames+ingredientNames+Garnishes+History+Notes (Recipe has no separate "description" field per data-model.md, so History/Notes stand in); `RecipeSearchDocumentRefresher` (Infrastructure) orchestrates it against real data and is also the idempotent/re-runnable backfill (`RefreshAllAsync`) — invoked by both a new standalone `Tools/SearchBackfill` console app (for an already-deployed DB) and by Seeder (for a freshly-seeded one). **First real outbox producer/consumer in the codebase**: `Ingredients.Contracts.Events.IngredientRenamed`, a minimal `IngredientRenameService` (rename mechanism only — no public PUT endpoint; see new deferred task below), and Catalog's `IngredientRenamedSearchDocumentHandler`. Building this surfaced a real bug: `OutboxSaveChangesInterceptor` was DI-registered in all 10 modules but never actually attached via `options.AddInterceptors(...)` on any module's `AddDbContext` call — the outbox had never fired for anything. Fixed identically across all 10 modules (Catalog, Ingredients, Equipment, Identity, Authorization, Compliance, Media, Notifications, Glossary, Measurements), since every one had the same latent gap. The hierarchy-descendant walk (`IIngredientLookupPort.GetDescendantIdsAsync`) is an in-memory BFS over the whole ingredient table (small at V1 scale, revisit with a recursive CTE if that changes). The `uses` facet on `/recipes` mirrors the existing equipment/glassware in-memory post-filter pattern exactly; on `/search` it's a post-filter after the adapter's own pagination (documented limitation: a page can return fewer than `limit` items when the facet removes matches — full facet-aware pagination is T148/T149's explicit job, still open). New acceptance feature `US01_SearchReconciliation.feature` (own `[Binding]` class/state, step wording deliberately distinct from `US01DiscoverRecipesSteps` per the established cross-binding-state trap) — found and fixed a real test-isolation bug during development: the rename scenario originally reused an ingredient scenarios 1/3 also searched for, so the suite's pass/fail depended on scenario execution order; fixed by giving the rename scenario its own dedicated ingredient/recipe pair. `SpecPourWebApplicationFactory` now configures a fast (200ms) test-only outbox polling interval so the rename-refresh scenario can poll-and-assert within ~10s instead of the production 5s default. Verified end-to-end against the live docker-compose stack (rebuilt + migrated + reseeded): `GET /search?q=rum` surfaces Daiquiri, Hemingway Daiquiri, and Mai Tai exactly as the smoke-test rider specifies. All backend suites green: Unit 12/12 (new `SearchDocumentComposerTests`), Integration 1/1, Contract 12/12, Acceptance 18/18 (up from 13; ran twice to confirm no flakiness after the ordering fix). New deferred task (mirrors T153's treatment): a public `PUT /ingredients/{id}` rename endpoint — `IngredientRenameService` is the core a future endpoint would call, but building the endpoint itself (validation, authz, curator UX) is out of T155's scope and belongs with a future ingredient-authoring story.
- [X] T156 [US1] Entity info popovers on recipe detail (2026-07-12 human review; **scope confirmed by John — proceed as scoped**): tapping any ingredient line, glassware, or equipment chip opens a popover with the entity's description/how-to-use (data already in T037 endpoints and current DTOs) plus a link to its full entry; build the popover widget as THE reusable pattern T098 later adopts for glossary terms (popover-first — see T098's presentation guidance); does not wait for Phase 10, data dependencies already met, in frontend/app/lib/features/discover/recipe_detail/
  - Built as a generic `EntityInfoPopover` (frontend/app/lib/features/discover/entity_info/) driven by an `EntityInfoWatch` closure (`(ref) => ref.watch(someProvider(id))`) rather than a typed provider reference — Riverpod 3.3.2's `ProviderListenable` isn't part of flutter_riverpod's public export surface, so the closure form is what T098 should copy. New `IngredientDetailScreen` (/ingredients/:id) and `EquipmentDetailScreen` (/equipment/:id) are the "full entry" destinations; glassware and equipment share the Equipment module/API (a recipe's glassware IDs are Equipment IDs), so one screen/provider serves both. Ingredient lines and glassware/equipment (now individual tappable chips, previously a joined Text string) wired into recipe_detail_screen.dart. Widget coverage: scenario 5 in us01_discover_widget_test.dart (ingredient popover -> full entry -> back -> glassware chip popover -> full entry), plus the `expectNoRawLocalizationKeys` assertion on both new detail screens. Full frontend suite green (25/25).
- [X] T157 [US1] (bug, 2026-07-12 walkthrough) Raw localization keys rendered in the Discover browse list — subtitles showed "family.sour"/"family.flip" (unresolved i18n keys, constitution Principle VIII violation; facet chips resolved correctly, so per-widget wiring). Fixed: family taxonomy keys and allergen keys (same defect class, visible in the new FR-055 walkthrough items) now resolve through core/l10n/content_key_labels.dart (known keys → ARB entries; curator-added unknown keys → humanized fallback, never key-shaped); the facet chips' previously HARDCODED English labels also moved to ARB. Class-prevention: `expectNoRawLocalizationKeys` rendering assertion (no visible Text may match the key-shaped pattern `^(?!\d)\w+(\.\w+)+$`) added to the widget suite and the integration suite (twin helpers under test/support/ and integration_test/support/)
- [X] T158 [US1] (bug, 2026-07-12 walkthrough) Garnish contradiction on Daiquiri detail ("Garnish: None (served up)" followed by "Lime wheel"). **Determination: RENDERER bug, not seed content** — recipe_detail_screen.dart rendered `recipe.iceSpec` under the Garnish label (T042 defect); seed content was correct (garnish "Lime wheel", ice "None (served up)"). Fixed: separate labeled "Ice: …" and "Garnish: …" lines with regression assertions in the widget suite. No launch-content QA note needed
- [ ] T153 [US1→deferred] Human-readable SEO slugs for public content pages (gap review 2026-07-11): T044 shipped /pages/{recipes,concepts,glossary/terms}/{id} keyed by entity ID (fully crawlable/shareable, satisfies FR-004b's letter). Keyword slugs (e.g. /pages/recipes/mai-tai) are better for SEO but need a stored, unique, collision-handled slug column that interacts with recipe rename/edit (US3+) and old-slug redirects — deferred out of the MVP rather than built speculatively. Land it alongside the personal-library authoring story (US3, Phase 5) where recipe create/rename already touches these fields, or a dedicated SEO-hardening pass; add a slug column + backfill + a redirect-on-rename test when built.
- [ ] T159 [US1→deferred] Public `PUT /ingredients/{id}` rename endpoint (gap review 2026-07-12, from T155): T155 built `IngredientRenameService` (backend/src/Modules/Ingredients/Infrastructure/) as the minimal internal rename mechanism needed to prove the event-maintained search-document refresh pipeline (ADR-0002) end-to-end — it's the core a real endpoint would call, but a curator-facing PUT endpoint (request validation, authz, audit, the actual editing UX) is ingredient-authoring scope, not search-reconciliation scope. Land it alongside a future ingredient-authoring story; the rename→event→refresh mechanism is already built and tested (US01_SearchReconciliation.feature, Scenario 5) and needs no further work when the endpoint is added.
- [ ] T152 [US7] Glossary auto-link rendering in the Flutter recipe/concept detail screens — SCHEDULED into Phase 10, not backlog (gap review 2026-07-11; scope decision confirmed with John; restyled [X]→[ ] 2026-07-14, /speckit-analyze U1): T042's own text named "glossary links with return navigation," but wiring content text through the T039 auto-link endpoint into clickable inline spans + a glossary-term detail screen + scroll-position-preserving return navigation is spec §5 / FR-027 requirement work that belongs with the glossary reader feature, not a standalone bolt-on. The deliverable is Phase 10's existing T098 (now explicitly including the US1 recipe/concept detail surfaces), and the "deep-link from mid-recipe → back → exact scroll position" acceptance test is folded into T095 — **implementation happens at the Phase 10 checkpoint, tracked there, not here.** (Previously marked [X] "only to record the scheduling decision" — restyled unchecked per standing convention: `[X]` means done, full stop; a scheduling decision is recorded in prose, never as a checked box, so progress counts stay honest.)

**Checkpoint**: MVP — guest discovery fully functional and demoable

---

## Phase 4: User Story 2 — Create an Account and Manage Identity (P1)

**Goal**: Consumer-scale registration, sign-in, MFA, recovery, sessions, lifecycle, DOB-as-sensitive-PII

**Independent Test**: Register (of-age + underage paths), social sign-in, MFA, recovery, session revoke, export, delete; verify DOB ciphertext, predicate-only exposure, audited decrypt

- [X] T045 [P] [US2] Acceptance tests (failing first) for US2 scenarios 1–8 in backend/tests/Acceptance/Features/US02_Identity.feature
  - Sub-checkpoint 1 (2026-07-13): all 8 scenarios written; 5/8 GREEN (1,2,3,7,8 — registration, underage rejection, sign-in, default-tier entitlements, audited age predicate). Scenarios 4/5/6 (recovery, export, deletion) correctly RED — 404, gated on T050/T053 landing in later sub-checkpoints. Own [Binding] class/step vocabulary per the established cross-binding-state convention. Includes a from-scratch C# authorization-code+PKCE token-acquisition helper (AcquireAccessTokenAsync) standing in for the Flutter client until T055 — this is what proved ADR-0003's flow actually works end-to-end and surfaced the hybrid cookie/bearer auth-scheme bug T047 fixed.
- [X] T146 [US2] Real email channel adapter behind IEmailChannelAdapter: dev SMTP mail-catcher (docker-compose service) for local/CI, production provider selected by configuration per the adapter rule, verified by the existing per-port contract tests, in backend/src/Modules/Notifications/Infrastructure/Email/ and docker-compose.yml
  - Done (2026-07-13): `SmtpEmailChannelAdapter` (MailKit — System.Net.Mail.SmtpClient is obsolete/unsupported for new development), config-driven via `SmtpEmailOptions` (`Email:Smtp:*`). One adapter, not per-provider adapter classes — most transactional-email providers (SendGrid, Postmark, SES, etc.) speak SMTP, so "production provider selected by configuration" is satisfied by pointing the same adapter at a different host/port/credentials; a provider needing a non-SMTP API would get its own adapter later. `docker-compose.yml` gained a `smtp4dev` service (ports 2525 SMTP / 5080 web UI to view caught mail) wired into the `api` service's env/depends_on. New `SmtpEmailChannelAdapterContractTests` (Testcontainers-managed smtp4dev, not docker-compose's) passes the exact same `EmailChannelAdapterContractTests` base `LoggingEmailChannelAdapterContractTests` does — proving the swap is safe. `LoggingEmailChannelAdapter` is now test-only: the shared `SpecPourWebApplicationFactory`/`ComposedHostFixture` hosts override back to it (no reachable SMTP server there, same pattern as their `IClock` override) so acceptance/contract scenarios that don't specifically test email don't fail on an unrelated connection error. Verified live: full docker-compose stack rebuilt, `specpour-smtp4dev` healthy, web UI reachable at :5080.
- [X] T046 [P] [US2] Flutter integration test for register/sign-in/MFA/sessions/lifecycle in frontend/app/integration_test/us02_identity_test.dart
  - Done (2026-07-14): scenarios 4 (view active sessions) and 5 (deactivate then reactivate) added, completing this file's register/sign-in/MFA/sessions/lifecycle scope. Built the three missing Flutter screens these scenarios (and the T051-T053 spec) needed first — none existed before this task: `sessions_screen.dart` (list + per-session revoke), `account_lifecycle_screen.dart` (deactivate with a confirm dialog, reactivate), `account_data_screen.dart` (export showing the sole raw-DOB display surface in the app, delete with a confirm dialog navigating home on success) — plus the `IdentityAuthService` methods wrapping the corresponding generated API client calls (`listSessions`, `revokeSession`, `deactivateAccount`, `reactivateAccount`, `exportAccountData`, `deleteAccount`, and T163's still-missing `regenerateBackupCodes`) and `Session`/`MeExport` route wiring (`/account/sessions`, `/account/lifecycle`, `/account/data`) in `app_router.dart`. None of the three screens are reachable from any navigation chrome yet — same flagged gap as `mfa_settings_screen.dart` (T161) — direct-route only. New `test/features/identity/account_management_widget_test.dart` (4 sandbox-runnable widget tests covering all three screens' happy paths, fake-Dio-interceptor convention) since `integration_test/us02_identity_test.dart` itself needs a real device/browser this sandbox doesn't have (unrunnable here, `flutter analyze` clean is the available verification). Dart client regenerated as part of T151's pass in this same session picked up `Session`/`SessionList`/`MeExport` (T051-T053's schemas, previously generated-but-stale). New l10n strings for all three screens in `app_en.arb`. Full regression: `flutter analyze` clean, `flutter test` 50/50 (+4 new), `flutter build web` succeeds. Backup-code UI (T163's own remaining gap) and social sign-in/login-requires-MFA integration scenarios remain out of scope — the latter two per the file's own long-standing doc comment (T165 tracks the fake-OAuth-handler alternative for social sign-in). **Scope note (John's directive, 2026-07-14)**: building these three screens here was an adaptive call (the integration scenarios needed something to drive) that relocated scope originally listed under T055 without updating that entry at the time — see T055's own re-baselined text and new sub-bullet for the correction.
  - Register/sign-in scenarios written (2026-07-13); scenario 3 (MFA enrollment) added alongside T050's Flutter UI (2026-07-13) — login-requires-MFA isn't covered (needs a sign-out affordance that doesn't exist until T051/T052) and social sign-in isn't covered (needs real provider credentials this environment doesn't have). Sessions/lifecycle scenarios land alongside those sub-checkpoints. Navigates directly via the router (no gated action exists yet on Discover to reach these screens through, per T043's own note). Not runnable in this sandbox (no device/browser) — same limitation as the existing scenarios.
- [X] T047 [US2] Registration with DOB capture, jurisdiction-aware underage rejection with zero persistence (FR-002c), client-side retry flagging contract in backend/src/Modules/Identity/Application/Registration/
  - Backend done (2026-07-13): POST /auth/register + POST /auth/login (AuthEndpoints.cs), reusing Compliance's jurisdiction-age rule via a new ILegalDrinkingAgePort (contract-swept out of AgeGateEndpoint's inline logic so both surfaces agree). Underage rejection (403) persists nothing — verified by an acceptance-test DB query asserting no user row exists. Rate limiting is the existing Phase 2 anonymous limiter (no new throttling needed); client-side retry flagging is T055's job. **Two real bugs found and fixed**: (1) ASP.NET Core Identity's default password-complexity rules (digit/upper/lower/symbol) were still active despite only RequiredLength=12 being set in T017 — relaxed to length-only per NIST 800-63B ("modern credential handling," FR-001) since arbitrary composition rules were never an intentional V1 decision, just an unexercised default. (2) SignInManager signs in under `IdentityConstants.ApplicationScheme` by hardcoded convention, but T017's cookie scheme was registered under the generic `CookieAuthenticationDefaults` name and TokenEndpoints authenticated against that same generic name — renamed both to `IdentityConstants.ApplicationScheme` so registration/login's cookie and /connect/authorize's redemption of it agree; then discovered a THIRD, subtler bug this rename exposed — endpoints that read `httpContext.User` without an explicit scheme (e.g. GET /me/entitlements) only ever saw the cookie scheme's view, silently ignoring a valid `Authorization: Bearer` header. Fixed with the standard ASP.NET Core hybrid-auth `AddPolicyScheme` pattern (bearer header present → OpenIddict validation scheme, else → cookie scheme) as the actual default scheme. Auth-flow design recorded as **ADR-0003** (first-party cookie login + client-driven PKCE exchange, not a password grant on /connect/token). Email confirmation (`RequireConfirmedEmail=true`, set in T017, no verification flow ever built) auto-confirms for V1 — flagged as new deferred task **T160**. New `contracts/openapi/paths/identity.yaml` (RegisterRequest/LoginRequest/AuthAccount — deliberately no dateOfBirth or token in any response) plus a new `SensitivePiiInvariantTests` contract test scanning the whole bundled OpenAPI doc for any stray `dateOfBirth` response property. Frontend registration/sign-in screens are T055's remaining scope.
- [X] T048 [US2] Derived age predicates internal contract IsOfLegalDrinkingAge(userId, jurisdiction) + age-band projection; audited decrypt on every raw access in backend/src/Modules/Identity/Contracts/
  - Done (2026-07-13): `IAgePredicatePort` (Identity.Contracts) + `AgePredicateAdapter` — every decrypt writes an `AuditEntry` (Authorization.Contracts.Audit) before returning a result, verified by an acceptance test querying `AuthorizationDbContext.AuditLogEntries` directly. `GetAgeBandAsync` returns standard demographic bands (under18/18-24/25-34/.../65+), never an exact age or DOB. Shares a new pure `AgeCalculator.CalculateAge` helper (Identity.Application, unit-tested: birthday-passed/not-yet/exact-today/Feb-29 cases) with T047's registration underage check, so both compute age identically.
- [X] T049 [P] [US2] External OIDC social sign-in adapters (Google, Apple, Microsoft) behind IExternalIdentityProviderPort in backend/src/Modules/Identity/Infrastructure/ExternalProviders/
  - Done (2026-07-13): a real browser-redirect OAuth handshake (`GET /auth/external/{provider}` (+`/callback`), `POST /auth/external/complete-registration`), NOT a "client submits an already-acquired provider token" design — api-v1-surface.md's own row (`GET ... (+callback)`) specifies the redirect shape. Uses each provider's own first-party/community ASP.NET Core handler (Google/MicrosoftAccount official Microsoft packages; Apple via aspnet-contrib's `AspNet.Security.OAuth.Apple`, since there's no first-party one and hand-rolling Apple's JWT-client-secret + JWKS validation would be exactly the kind of security-sensitive reinvention R6 exists to avoid) rather than hand-rolled JWT validation. `IExternalIdentityProviderPort` (one adapter per provider) is narrower than originally envisioned: token validation is the proven handler's job, so the port's only responsibility is normalizing each provider's differently-shaped claims into a common (Subject, Email, DisplayName) — genuinely earns its keep for Apple, which only ever supplies email/name on a user's very first authorization. `SignInManager.GetExternalLoginInfoAsync()` (ASP.NET Core Identity's own built-in mechanism for this exact "interim principal between provider redirect and app decision" step) drives account lookup/linking-by-email; MFA enforcement is NOT bypassed by the built-in `ExternalLoginSignInAsync` shortcut — factored T050's `LoginAsync` MFA-branch into a shared `AuthEndpoints.SignInOrChallengeMfaAsync` reused by both the password and social sign-in paths, so a user can't dodge their own enabled MFA by using Google/Apple/Microsoft instead. Brand-new social accounts can't be created immediately: FR-002/FR-002c apply identically to social registration (spec.md US2's own wording, "registers with email/password or social sign-in, provides their date of birth") and no provider reliably supplies DOB (Apple deliberately never does), so a first-time sign-in redirects with `needsDateOfBirth=true` and the client completes registration via a second call using the still-pending `IdentityConstants.ExternalScheme` cookie. Real bug found and fixed: registering all three provider handlers unconditionally 500'd EVERY request in this (or any) environment without real credentials configured, because ASP.NET Core's remote-auth handlers eagerly validate a non-empty ClientId on every request (the auth middleware asks each registered scheme whether it owns the current path, as part of matching each OAuth handler's callback path) — not just when actually challenged. Fixed by registering each provider (scheme + adapter) only when its ClientId is actually configured; an unconfigured provider now cleanly reports "unknown provider" (400) instead of crashing. Also fixed a shared test-harness bug this surfaced: `OpenApiResponseValidator`'s path matcher picked the first template match in document order, so `POST /auth/external/complete-registration` was shadowed by `GET /auth/external/{provider}` — fixed to prefer an exact literal path match, same precedence real ASP.NET Core routing already has (confirmed no actual runtime ambiguity, only the test validator's simpler matching had it). **Caveat disclosed to and accepted by John**: no real Google/Apple/Microsoft app credentials exist in any environment here, so the actual OAuth handshake, account-linking, and DOB-completion branches in `ExternalAuthEndpoints` are NOT exercised end-to-end by an automated test — only what's testable without real credentials (each adapter's claims normalization, and the two endpoints' behavior when no provider is configured / no pending sign-in exists) is covered. Full end-to-end verification tracked as new deferred task **T165** (fake-OAuth-handler test harness, John's preferred direction over staging credentials — /speckit-analyze C2, 2026-07-14). Full regression: Unit 26/26 (new `ExternalIdentityProviderAdapterTests`), Integration 1/1, Contract 27/27, Acceptance 26/28 (still only 5/6 export/deletion correctly red).
- [X] T050 [P] [US2] TOTP MFA enrollment/disable + secure recovery flow (email via notifications module) in backend/src/Modules/Identity/Application/{Mfa,Recovery}/
  - Done (2026-07-13): MfaEnrollment entity (data-model.md: id/user_id/method/secret reference/enabled_at) with its own AES-256-GCM cipher (DataProtectionMfaSecretCipher, same key-ring pattern as the DOB cipher, distinct purpose string) — NOT ASP.NET Identity's built-in TwoFactorEnabled/AuthenticatorKey columns, since the data model calls for a real owned entity. Pure TOTP logic (TotpCodeGenerator, Otp.NET) in Identity.Application/Mfa/, mirroring AgeCalculator's shape. GET/POST/DELETE /me/mfa (Endpoints/MfaEndpoints.cs): POST is two-phase over one endpoint (no code = start enrollment/issue secret; code = confirm) to match the literal 3-verb api-v1-surface.md row rather than adding a 4th endpoint. LoginAsync switched from PasswordSignInAsync to CheckPasswordSignInAsync so an MFA-enabled account never gets a full cookie before the code is verified; when MfaEnrollment is enabled it instead signs the caller into a new short-lived cookie scheme (IdentityModule.MfaPendingScheme, 5 min) and returns `{requiresMfa:true}` (new LoginResponse shape — login's 200 schema changed from AuthAccount to LoginResult). New POST /auth/login/mfa reads that pending cookie, verifies the code, and completes the real sign-in — not literally in api-v1-surface.md's table but necessary the same way ADR-0003's endpoints were designed by judgment where the surface doc is a catalog, not exhaustive. Recovery (RecoveryEndpoints.cs) uses UserManager's own Generate/ResetPasswordTokenAsync (unrelated to the custom TOTP secret storage) — POST /auth/recovery always 202s regardless of whether the email exists (no enumeration), POST /auth/recovery/confirm resets the password. Architectural correction made in passing: IEmailChannelAdapter (T023/T146) lived in Notifications.Application.Ports, which recovery's cross-module call would have violated Principle III's Contracts-only pattern to reach — moved it to Notifications.Contracts (its doc comment already anticipated being "identity transactional mail"'s port) before Identity.Infrastructure took a ProjectReference to it, matching ILegalDrinkingAgePort/IAgePredicatePort's established location. FR-064 (MFA mandatory at token issuance for platform-scoped roles) deliberately NOT implemented here — flagged as staff-console territory (T079-T084, where RoleGrants are actually administered) rather than bolted onto consumer-facing T050. Two new acceptance scenarios added (US02_Identity.feature 9/10: enroll-then-required-at-login, disable) using a new TotpCodeGenerator.ComputeCurrentCode to stand in for an authenticator app; scenario 4 (recovery) now genuinely green instead of the placeholder 404 check. Full regression: Unit 22/22, Integration 1/1, Contract 25/25 (new OpenAPI paths hit an OpenAPI-3.1-vs-3.0 bug of my own making — `nullable: true` isn't a 2020-12 keyword, fixed to `type: [string, "null"]`), Acceptance 26/28 (only 5/6 export/deletion still correctly red, gated to T053).
- [ ] T165 [US2→deferred] End-to-end social sign-in test coverage (gap review 2026-07-14, from T049; /speckit-analyze C2): T049's actual OAuth handshake, account-linking, and DOB-completion branches in `ExternalAuthEndpoints` have zero end-to-end coverage — no real Google/Apple/Microsoft app credentials exist in any environment this codebase has run in, so only what's testable without them (adapter claims normalization, provider-not-configured behavior) is covered today. **Preferred approach (John, 2026-07-14): a fake-OAuth-handler test harness**, not staging credentials — repeatable in CI, no dependency on live third-party accounts or secrets management for test infrastructure. Build a test-only `AuthenticationHandler<TOptions>` substituted for each real provider scheme in the test host, driven by a controllable fake `ClaimsPrincipal`/claims set, so `CallbackAsync`'s existing-user/link-by-email/new-user-needs-DOB branches and `CompleteRegistrationAsync` can all be exercised as real acceptance scenarios. Land alongside a future OAuth/identity hardening pass, or whenever T049's real branches are next touched. **Boundary with T173 (explicit, D1, 2026-07-17)**: T173 absorbed only the config-driven-provider-rendering slice of this task's original intent (dead-button prevention when a provider is unconfigured) — that slice is T173's, not T165's, and is NOT re-scoped here. T165's remaining, un-absorbed scope is the fake-handler test harness itself: exercising `CallbackAsync`'s existing-user/link-by-email/new-user-needs-DOB branches and `CompleteRegistrationAsync` end-to-end, which T173 explicitly leaves "untestable until real credentials exist." The two tasks are complementary, not duplicates: T173 productionizes what's visible without credentials; T165 tests what's currently only reachable with them.
- [ ] T160 [US2→deferred] Email verification/confirmation flow (gap review 2026-07-13, from T047): T017 set `SignIn.RequireConfirmedEmail = true` with no corresponding verification flow ever scheduled — enforcing it as-is would permanently lock every new registrant out of sign-in with no way to unblock. T047's AuthEndpoints.RegisterAsync auto-confirms email at registration for V1 (same as the Super Admin bootstrap already did), consistent with FR-001's "modern credential handling" not explicitly requiring email verification and no task in this plan ever having scoped one. Build a real flow (generate token via UserManager.GenerateEmailConfirmationTokenAsync, send via T146's real email adapter, a confirm-email endpoint) when there's a task that actually needs it — likely bundled with T050 (recovery already needs transactional email) or its own hardening pass before launch.
- [X] T161 [US2] Account/settings navigation shell — **first item of Phase 4 sub-checkpoint 4** (John's directive, 2026-07-14: no walkthrough can exercise any of these screens until this exists; reachability of all four becomes a walkthrough checklist line, ahead of T163's backup-code UI). Originally gap-reviewed 2026-07-13 against just `mfa_settings_screen.dart`; now spans FOUR unreachable screens, not one — `mfa_settings_screen.dart` (T050, `/account/mfa`), `sessions_screen.dart` (T046, `/account/sessions`), `account_lifecycle_screen.dart` (T046, `/account/lifecycle`), `account_data_screen.dart` (T046, `/account/data`) are all real, complete, and directly-routable, but the app has no account/settings navigation chrome at all (no drawer, tab bar, or menu — Discover's AppBar is still the only nav surface, per T043's own note) linking to any of them. Scope: a real entry point (account menu/drawer item) plus whatever minimal shell/landing screen makes the four destinations discoverable — not a full settings redesign. T151's still-unbuilt channel-preferences screen and T163's backup-code UI (T055's remaining scope) should get entry points here too when they land, rather than repeating this gap a third time.
  - Done (2026-07-14): new `account_menu_screen.dart` (`/account`) — a minimal `ListView` of five destinations (MFA, sessions, notification preferences, your data, deactivate account), each a `ListTile` pushing its existing route; deliberately not a settings redesign. Entry point: a new `accountNavButton` icon in `DiscoverScreen`'s `AppBar` (matching the existing `aboutNavButton`'s pattern exactly), routed through `requireAccount` (T043's existing guest-gate helper) — a guest tapping it gets the sign-in prompt with the captured intent resuming into `/account` on sign-in, same as any other gated action; no new gating mechanism invented. **Also built the channel-preferences screen** (`channel_preferences_screen.dart`, `/account/channels`) per John's explicit fold-in instruction — T151's `GET`/`PUT /me/channels` had an entry point to wire but no destination screen yet; building it now means T161 doesn't link to a broken route, and it closes T055's second-to-last gap in the same pass (see T055's own note — only T163's backup-code UI remains open there). New tests: `account_menu_widget_test.dart` (5 cases, one per destination, verifies each `ListTile` actually navigates), `channel_preferences_widget_test.dart` (list + toggle against a fake Dio interceptor, same convention as `account_management_widget_test.dart`), plus one new case in `us01_discover_widget_test.dart` confirming the account button is genuinely guest-gated (shows the sign-in prompt, not the account menu, for an anonymous visitor). Full regression: `flutter analyze` clean, `flutter test` 57/57 (+7), `flutter build web` succeeds. Native platform reachability (a drawer/tab bar beyond the AppBar icon) not addressed — out of this task's "not a full settings redesign" scope; the AppBar icon works identically on every target platform this sandbox can build (web) and should on the others, un-verified beyond that per the established no-device-here limitation.
- [X] T162 [US2] Security verification of MfaEnrollment secret storage (John, 2026-07-13; requested as "T159" but that ID was already taken by the deferred ingredient-rename task above — recorded here as T162). Architecture (dedicated entity + application-level AES-256-GCM) retroactively APPROVED; this was the 8-item GCM-pitfall audit. Results: (1) nonce uniqueness PASS — .NET 10's `AesGcmAuthenticatedEncryptor` (always selected for `AES_256_GCM` config on modern .NET, verified against aspnetcore source) generates a fresh random 96-bit nonce AND a random 128-bit key modifier per Protect call, the modifier deriving a per-operation subkey via SP800-108, both stored in the payload — unique (key, nonce) pairs beyond the checklist's bar; observable via the `Two_encryptions_..._produce_different_ciphertext` unit test. (2) platform primitive PASS — `System.Security.Cryptography.AesGcm` inside the framework encryptor; corroborated live by tamper/swap tests failing with `AuthenticationTagMismatchException`, a type only AesGcm throws. (3) SPLIT — rotation/key-ID PASS (every Data Protection payload embeds `magicHeader || keyId(GUID)`; Unprotect selects the key by embedded id, so rotation needs no flag day and the "key-ID must exist now" prerequisite is satisfied per-row, tamper-protected under the same AAD), but key CUSTODY FAIL→deferred: the key ring persists via `PersistKeysToDbContext<IdentityDbContext>` with no `ProtectKeysWith*`, i.e. effectively plaintext in the same database as the ciphertexts; no secrets/KMS port exists anywhere in the codebase yet, and a local cert-wrap would just move the plaintext one hop (cert password in env). Pre-existing posture shared with the DOB cipher since Phase 2 — deferred to T139's hardening pass (see its new sub-bullet), same precedent as the OpenIddict ephemeral-cert note in the same file. (4) AAD binding FAILED→FIXED: the purpose string was fixed per-module, and DP purposes ARE the GCM AAD (`magicHeader || keyId || purposeCount || purposes*`, KeyRingBasedDataProtector) — so a ciphertext copied onto another user's row decrypted fine. `IMfaSecretCipher` now takes the owning userId on both operations and the implementation chains it into the purpose (`CreateProtector(Purpose, userId)`); cross-row swap now fails the GCM tag check (proven by unit test). No re-encryption migration needed — nothing committed/deployed contains MfaEnrollment ciphertexts (only throwaway dev/test rows predate the change). (5) recovery codes N/A — none exist: T050's recovery is ASP.NET Identity's stateless password-reset token (HMAC-bound to the security stamp, never stored); no MFA backup codes were ever scoped → lockout gap flagged as T163. (6) plaintext containment PASS — decrypts only in MfaEndpoints (confirm) and AuthEndpoints.LoginMfaAsync, both module-internal; IMfaSecretCipher lives in Application.Ports, not Contracts; zero logging statements in the Identity module, no HTTP body-logging middleware; the start-enrollment response is the single, inherent, documented secret-bearing surface (TOTP requires delivering the secret to the authenticator once) and it is never re-shown. (7) negative-path tests ADDED: tampered-ciphertext + cross-user-swap unit tests running under the production AES-256-GCM algorithm config (test provider rebuilt to match IdentityModule's exact `AuthenticatedEncryptorConfiguration`, not the Create() default of CBC+HMAC), plus a live-HTTP acceptance step ("the enrollment secret is never shown again", scenario 9) asserting the confirm response and GET /me/mfa never carry the secret. (8) audit: state transitions now audited (`identity.mfa_enabled` on confirm, `identity.mfa_disabled` on a disable that actually removed an enabled enrollment) through the shared IAuditWriter port, actor=subject per the AgePredicateAdapter convention; per-login decrypts deliberately EXEMPT — Principle XIV's append-only audit covers administrative/staff actions, and a row per sign-in would turn the staff-review table into a login-activity ledger (that visibility is T051 SessionDevice + OTel territory); no staff surface reads MfaEnrollment today, and when staff MFA-reset lands (T083/T163) that action must be audited. Full regression after fixes: Unit 29/29, Integration 1/1, Contract 27/27, Acceptance 26/28 (same two T053-gated reds).
- [X] T163 [US2] MFA lockout recovery (gap review 2026-07-13, from T162's audit item 5; APPROVED and scoped by John 2026-07-13, promoted from deferred)
  - Done (2026-07-14): **Spec reconciliation first** — specification-statement.md §1 did NOT actually contain John's described "password reset must never bypass an MFA gate"/"MFA loss must never be permanent lockout" wording when checked (git history showed no such edit landed); rather than silently fabricate or silently skip, added the substance directly to spec.md as new **FR-001a** plus two new acceptance scenarios (spec.md US2 scenarios 9/10) using John's own wording verbatim, and flagged the statement-doc discrepancy rather than editing that document myself (established pattern: John edits source statements, not me). New `MfaBackupCode` entity (id/user_id/code_hash/created_at/used_at, migration `AddMfaBackupCodes`) — hashed via ASP.NET Core Identity's own `IPasswordHasher<ApplicationUser>` (the same mechanism the account password uses), never encrypted, per the T162 checklist's own rule that recovery codes are one-way-verified, not reversible. Pure `BackupCodeGenerator` (Identity.Application/Mfa/, 10 codes, "XXXX-XXXX" format, ambiguous characters 0/O/1/I/L excluded for hand-typing) mirrors `TotpCodeGenerator`'s shape. Shared `BackupCodeStore` (Endpoints/, generate+consume) used by both MfaEndpoints and AuthEndpoints — same cross-file-internal-helper pattern as `AuthEndpoints.SignInOrChallengeMfaAsync`. The initial set is issued exactly once, bundled into the confirm-that-enables response (`POST /me/mfa`'s `MfaEnrollmentResponse.backupCodes`) — never a separate enrollment step; a new `POST /me/mfa/backup-codes` regenerates on demand, invalidating every prior code (used or not); `DELETE /me/mfa` also clears backup codes (meaningless without an active enrollment). `POST /auth/login/mfa` now tries the TOTP code first, falling back to backup-code consumption (hash-compared against the small unused set — no lookup index possible for a salted hash) — consumption is audit-logged (`identity.mfa_backup_code_consumed`), unlike routine TOTP decrypts, since a consumed recovery code is exactly the "was this factor lost" signal T162's audit posture cares about; generation is audit-logged too (`identity.mfa_backup_codes_generated`). Password-reset-never-bypasses-MFA (FR-001a's first half) needed NO code change — `RecoveryEndpoints.ConfirmAsync` only resets the password hash via `ResetPasswordAsync`, never signs the caller in, so the normal `POST /auth/login` → `SignInOrChallengeMfaAsync` MFA gate was already structurally unbypassable; verified explicitly by a new acceptance scenario rather than left as an inference. Four new US02_Identity.feature scenarios (11-14: backup-code sign-in, single-use rejection on reuse, regeneration invalidates the prior set, password-reset-never-bypasses-MFA) — scenario 14's password reset drives `UserManager.GeneratePasswordResetTokenAsync` directly in the test (no email-capture harness exists) rather than through the real email transport, exercising the identical `ResetPasswordAsync` code path the endpoint calls. Full regression: Unit 35/35 (+4 `BackupCodeGeneratorTests`), Integration 1/1, Contract 28/28 (+1), Acceptance 30/32 (+4 new scenarios, still only the same 2 T053-gated reds).
- [X] T164 [US2] DOB cipher AAD user-binding (John, 2026-07-13, immediate priority after T162: "the same zero-data window that let the MFA fix land without a re-encryption migration applies to the DOB cipher only until real data is first committed"). Mirrors T162 item 4's fix exactly: `IDateOfBirthCipher.Encrypt/Decrypt` now take the owning `Guid userId` and `DataProtectionDateOfBirthCipher` chains it into the Data Protection purpose (`CreateProtector(Purpose, userId.ToString())`), which is the GCM associated data — a ciphertext copied onto another user's row now fails the AesGcm tag check instead of decrypting. Call sites updated: `AuthEndpoints.RegisterAsync` (`AuthEndpoints.cs:67-84`, id generated before the object initializer since `user.Id` isn't in scope inside its own initializer), `ExternalAuthEndpoints.CompleteRegistrationAsync` (`ExternalAuthEndpoints.cs:157-172`, same pattern), `AgePredicateAdapter.DecryptAndAuditAgeAsync` (`AgePredicateAdapter.cs:51`, uses the already-fetched `user.Id`), `Seeder/Program.cs` Super Admin bootstrap (`Program.cs:134-146`, same pattern as the two registration endpoints). No T053 export-flow call site exists yet (unimplemented). `DataProtectionDateOfBirthCipherTests.cs` rewritten to mirror `DataProtectionMfaSecretCipherTests.cs` exactly — round-trip/containment/uniqueness (existing) plus new tamper and cross-user-swap negative tests, provider rebuilt with the production `AuthenticatedEncryptorConfiguration{AES_256_GCM}` (not the `Create(appName)` default of CBC+HMAC) so the tamper test exercises the real primitive. No re-encryption migration needed — landed inside the same zero-committed-data window T162 used. Full regression: Unit 31/31 (+2 new), Integration 1/1, Contract 27/27, Acceptance 26/28 (same two T053-gated reds).
- [X] T051 [US2] Session/device management: SessionDevice entities, GET/DELETE /me/sessions, refresh-token family revocation in backend/src/Modules/Identity/Application/Sessions/
  - Done (2026-07-14): a "session" is one OpenIddict authorization — `SessionDevice` (migration `AddSessionDevice`) only adds the human-facing metadata (device description, created/last-seen) OpenIddict's own Authorization/Token tables don't carry; no separate token-family bookkeeping needed, since OpenIddict already IS the token family. Creation/refresh point: `TokenEndpoints.HandleTokenAsync` — the code/refresh-token principal always carries OpenIddict's own internal authorization id (`ClaimsPrincipal.GetAuthorizationId()`, the SAME id for every token minted off one authorization, confirmed against the installed `OpenIddict.Abstractions` 7.5.0 package's own XML docs before writing code that depends on it). An `authorization_code` grant means a brand-new session (insert); a `refresh_token` grant just means an existing one is still alive (update `LastSeenAt`). `GET /me/sessions` lists active (non-revoked) sessions; `DELETE /me/sessions/{id}` calls `IOpenIddictAuthorizationManager.TryRevokeAsync` directly on the underlying authorization — OpenIddict itself then refuses any further `refresh_token` grant against it, which is what actually kills the whole family, not a manual token-row sweep. Device description is the raw `User-Agent` header (best-effort, no parsing library — V1 scope). New acceptance scenario 15 (spec.md scenario 3: "sign in from a second device, both sessions appear, either can be revoked") — two independent `AcquireAccessTokenAsync()` calls against the same cookie session simulate two devices (each is a genuinely separate authorization+token exchange, which is what actually matters here, not two different HTTP clients). Full regression: Unit 35/35, Integration 1/1, Contract 30/30 (+2), Acceptance (same two T053-gated reds, new scenario green).
  - **Revocation-semantics correction (2026-07-14, applied while building T052, John's ruling)**: revoking a session here does NOT retroactively kill an already-issued access token — OpenIddict's access tokens are self-contained/stateless (no per-request DB check), so `TryRevokeAsync` only blocks a future `refresh_token` grant against that authorization. Scenario 15 originally only asserted the session-list side effect; added "And the first device's session can no longer be refreshed" (asserts the real, immediate guarantee — a `refresh_token` grant against the revoked authorization is rejected — rather than a false "the live access token dies instantly" claim). See T052's own done-note for the paired access-token-lifetime fix and T166 for the tracked gap this doesn't close.
- [X] T052 [US2] Account lifecycle: deactivate/reactivate with operator-configurable grace period (default 12 months), expiry warning notification event, scheduled deletion job in backend/src/Modules/Identity/Application/Lifecycle/
  - Done (2026-07-14): `LifecycleOptions` (pure POCO, `Application/Lifecycle/`) carries `GracePeriodDays=365`, `WarningDaysBeforeExpiry=30`, `PollingInterval=1h` (in-class defaults, config-bound under `Identity:Lifecycle` for operator override per FR-003's own wording). `AccountLifecycleBackgroundService` (`Infrastructure/Lifecycle/` — NOT `Application/Lifecycle/` as the task text suggested; Application has zero EF/DbContext dependency anywhere in this module, matching Otp.NET's own placement precedent, so a DbContext-touching hosted service can't live there) mirrors `OutboxDispatcherBackgroundService`'s poll-loop shape exactly (fresh DI scope per cycle, try/catch around the work). Two passes per cycle: warn accounts entering their warning window (raises `DeactivationExpiryApproaching`, records `DeactivationWarningSentAt` so it's one-shot), then delete accounts whose grace period fully elapsed via the SAME `AccountDeletionService` T053's `DELETE /me` uses. `LifecycleEndpoints.cs`: `POST /me/deactivate` (sets `LifecycleState=Deactivated`+`DeactivatedAt`, revokes every active `SessionDevice`'s underlying OpenIddict authorization — same mechanism as T051's single-session revoke, applied to all at once — raises `AccountDeactivated`, audits `identity.account_deactivated`), `POST /me/reactivate` (clears the deactivation fields, audits `identity.account_reactivated`; deliberately requires a fresh bearer token — deactivation revoked the old sessions — rather than silently reactivating on any password check, keeping it a deliberate user action per FR-003's "can reactivate" wording). Reuses the pre-existing `UserLifecycleState.Deactivated` enum value and `ApplicationUser.DeactivatedAt`/`DeactivationWarningSentAt` columns (both already anticipated this exact task). **Found and fixed a real, pre-existing gap while writing this task's own acceptance scenario**: Identity never registered `AccountDeactivated`/`DeactivationExpiryApproaching`/`AccountDeleted` with the process-wide `IEventTypeCatalog` (unlike Ingredients, which does via `IngredientsEventRegistrationHostedService`) — every one of these events' outbox rows was being dispatched to zero handlers forever (`OutboxDispatcherBackgroundService.Log.UnknownEventType`, left permanently pending), meaning T053's `AccountDeleted` producer and this task's two notification events were silently dead since the moment they were written. Fixed with a matching `IdentityEventRegistrationHostedService` (`IdentityModule.cs`). **Found and fixed a real security gap while writing this task's session-revocation assertion** (John's ruling, 2026-07-14, full disposition and 4 riders recorded in memory): revoking a session/deactivating an account only blocked future `refresh_token` grants — an already-issued access token (self-contained/stateless, no per-request DB check, OpenIddict's 1-hour default, never overridden) stayed live for up to an hour after "revocation." Fixed: `IdentityModule.cs` now calls `SetAccessTokenLifetime(TimeSpan.FromMinutes(10))`, bounding the exposure window while keeping the stateless validation path (rejected reference/opaque tokens as the wrong platform-wide cost for a low-severity window). Both T051's and T052's acceptance scenarios rewritten to assert the real, honest guarantee (refresh rejected immediately) instead of a false "live token dies instantly" claim; new `TokenLifetimeTests.cs` asserts the running config stays ≤10 minutes. Shortening the lifetime made refresh a hot path with **zero existing client-side handling** (verified: no reactive 401 handler, no proactive refresh timer, refresh_token was being discarded entirely) — added `frontend/app/lib/core/api/api_client_provider.dart`'s `TokenRefreshInterceptor` (a `QueuedInterceptor` catching a 401, exchanging the stored refresh token, retrying the original request transparently) and `RefreshToken`/`refreshTokenProvider`, plus `identity_auth_service.dart` now captures and persists the refresh token on every token exchange (previously discarded). New `frontend/app/test/core/api/token_refresh_interceptor_test.dart` (2 tests: successful silent refresh+retry, failed-refresh clears both tokens). True immediate invalidation for high-severity events (deactivation, staff suspension, sign-out-everywhere) tracked as new **T166** (below, launch-gated alongside T139) rather than attempted here — Rider 2's explicit call: additive middleware later, not foreclosed by this fix. Full regression: backend Unit 35/35, Integration 1/1, Contract 32/32, Acceptance 36/36 (+1 `TokenLifetimeTests`, all scenarios green); frontend `flutter analyze` clean, `flutter test` 46/46 (+2), `flutter build web` succeeds.
- [X] T053 [US2] Data export (sole raw-DOB surface) and deletion with public-attribution anonymization + rating-event de-identification events in backend/src/Modules/Identity/Application/Export/
  - Done (2026-07-14): `AccountDeletionService.DeleteAsync` (`AccountDeletionService.cs`) is the one deletion mechanism — raises `AccountDeleted` (Identity.Contracts/Events/, plain `DomainEvent`, NOT `INotificationEvent` since no user remains to notify; the intended future subscriber is the not-yet-built Community module's public-attribution anonymization/rating-event de-identification, data-model.md's own wording), then hard-deletes via `UserManager.DeleteAsync`, then audits `identity.account_deleted`. `GET /me/export` (`ExportEndpoints.cs`) returns `MeExportResponse` — the sole raw-DOB response surface in the platform (already allowlisted by name, `SensitivePiiInvariantTests.AllowedSchemaNames`), auditing `identity.dob_exported` on every call; also carries `mfaEnabled`, active sessions, and external-login providers. `DELETE /me` calls the same `AccountDeletionService`, shared with T052's future grace-period-expiry job. **Found and fixed a real architectural violation while building this**: `MfaEnrollment`/`MfaBackupCode`/`SessionDevice` (T050/T163/T051) had only indexed `UserId` columns, not real foreign keys — a deviation from ADR-0001's same-schema-real-FK rule that would have orphaned rows on hard delete instead of cascading; added the three FKs with `OnDelete(DeleteBehavior.Cascade)` (migration `AddUserForeignKeysAndLifecycleFields`, which also adds `ApplicationUser.DeactivatedAt`/`DeactivationWarningSentAt` ahead of T052). **Found and fixed a second real gap**: `GET /me/export` and `DELETE /me` had gone straight to code without ever being wired into `contracts/openapi/paths/identity.yaml`/`openapi.yaml` (`meExport`, `meById` paths, `MeExport` schema) — the endpoints worked but had no contract coverage; added the paths/schema and two new `OpenApiConformanceTests` (401-without-token, matching the established pattern). **Found and fixed a real test-isolation bug surfaced by this task's new anonymous-traffic scenarios** (recovery, export/deletion): running the full acceptance assembly (not filtered) pushed the shared host's cumulative anonymous request count over the anonymous rate limiter's 100/min budget (`AnonymousRateLimiterExtensions` — every `TestServer` request shares one "unknown"/loopback IP partition), spuriously 429ing 3 unrelated scenarios that passed in isolation; root-caused via full stack trace (`Expected: 202, Actual: 429`), not guessed. Fixed by giving `SpecPourWebApplicationFactory` an `enableRateLimiting` constructor flag (default `false`, a test-only override of a production concern, same pattern as its existing `IClock`/`IEmailChannelAdapter`/outbox-polling-interval overrides) that `RateLimitingTests.cs` explicitly opts back into (`enableRateLimiting: true`) on its own separately-constructed instance, preserving that test's real 429 assertion. Full regression: Unit 35/35, Integration 1/1, Contract 32/32 (+2), Acceptance 33/33 (all scenarios green, including the two previously T053-gated reds and the three rate-limiter-flaky ones).
- [ ] T054 [US2] Identity endpoints + OpenAPI paths for T051–T053's remaining surface — GET/DELETE `/me/sessions` (T051), account lifecycle actions (T052), `GET /me/export` + `DELETE /me` (T053) — in backend/src/Modules/Identity/Infrastructure/Endpoints/ and backend/contracts/openapi/paths/identity.yaml (re-baselined 2026-07-14, /speckit-analyze D1: the register/login/MFA/recovery/external-sign-in surface this task originally named is already delivered by T047/T049/T050/T163's own endpoint work and identity.yaml paths — executing the original text would double-track finished work)
  - T051 and T053's portions done alongside those tasks (2026-07-14): `meSessions`/`meSessionById`/`meExport`/`meById` paths and schemas all landed in `identity.yaml`/`openapi.yaml`. Only T052's account-lifecycle endpoint surface remains open.
- [X] T055 [US2] Flutter identity feature: registration (with DOB + underage rejection UX), sign-in, MFA, recovery, session list, preferences, lifecycle screens in frontend/app/lib/features/identity/ (re-baselined 2026-07-14: session-list/lifecycle/data-export-and-delete screens were delivered under T046 instead, not here — see that task's own note and the sub-bullet below; this task's true remaining scope is a channel-preferences UI screen for T151's `GET`/`PUT /me/channels` and T163's backup-code display/regenerate UI, neither of which has a screen yet)
  - **Channel-preferences screen DONE (2026-07-14, delivered under T161 instead — same relocation pattern as T046)**: `channel_preferences_screen.dart` (`/account/channels`) built alongside T161's nav shell so its new entry point wouldn't link to a nonexistent screen.
  - **Backup-code UI DONE (2026-07-14) — T055 now fully closed.** `mfa_settings_screen.dart` gained: the confirm-enrollment response's `backupCodes` (already returned by the backend since T163, never displayed) shown once via a new `_backupCodesToShow` state field and a dedicated section (`mfaSettingsBackupCodesText`, `SelectableText` for easy copying); a `mfaSettingsRegenerateBackupCodesButton` (visible whenever MFA is enabled, alongside Disable) calling the already-existing `IdentityAuthService.regenerateBackupCodes()` (added in T046's pass but never wired to a button) and showing the fresh set the same way; a `mfaSettingsBackupCodesSavedButton` to dismiss the one-time display, matching the backend's own "shown exactly once, never re-shown" contract — no way to view a set again after dismissing, by design. Disabling MFA also clears `_backupCodesToShow` client-side (the codes are meaningless without an active enrollment, mirroring `DELETE /me/mfa`'s own server-side clear). Two new `mfa_widget_test.dart` cases (confirm-then-show-codes-then-dismiss; regenerate-then-show-fresh-codes-then-dismiss), both asserting the exact code text and that the display key disappears after dismissal. Full regression: `flutter analyze` clean, `flutter test` 58/58 (+8 since T161's a50b068 — 2 MFA cases plus regenerated l10n scaffolding), `flutter build web` succeeds.
  - **Scope correction (2026-07-14, John's directive on c0ab473)**: this entry's own text still claimed "session list... lifecycle screens" as open after T046 delivered `sessions_screen.dart`/`account_lifecycle_screen.dart`/`account_data_screen.dart` (T046 built them because its integration scenarios needed something to drive — an adaptive call made without updating this entry at the time, the same double-tracking risk D1 flagged for T054). Corrected here rather than left implicit. "Preferences" in this task's original title referred to T151's channel/notification opt-in preferences specifically (not account settings generally) — that screen is still genuinely unbuilt, along with T163's backup-code UI.
  - Register/sign-in slice done sub-checkpoint 1 (2026-07-13, `800bfe4`). MFA/recovery/social-sign-in slice done 2026-07-13: `mfa_challenge_screen.dart` (login-requires-MFA code entry), `mfa_settings_screen.dart` (enroll/confirm/disable — real, complete, but not yet reachable from any navigation chrome since there's no account/settings shell in the app yet; routable directly at /account/mfa, flagged as a follow-on task to wire a real entry point), `recovery_request_screen.dart`/`recovery_confirm_screen.dart`, `social_sign_in_buttons.dart` (real browser-redirect navigation via url_launcher's webOnlyWindowName:'_self' — web-only for now, native needs a registered deep link this sandbox can't build/verify), `external_sign_in_callback_screen.dart` (routes on requiresMfa/needsDateOfBirth/error query params), `complete_external_registration_screen.dart` (the DOB-only completion form for brand-new social accounts). `IdentityAuthService.signIn()` now returns a bool (requiresMfa) instead of always completing sign-in. Dart API client regenerated from the updated OpenAPI doc (found+fixed a real `dart` PATH issue — the script assumes `dart` resolves to the Flutter SDK's copy, not a bare system one). Session list/preferences/lifecycle screens remain, pending T051-T053. Verification: `flutter analyze` clean, `flutter test` 44/44 (14 new: MFA login-challenge, MFA settings enroll/confirm/disable, recovery request/confirm, social sign-in buttons+callback+DOB-completion), `flutter build web` succeeds. Not visually click-through-verified (no browser in this sandbox, per the established Phase 3 pattern) — that's John's checkpoint to make, likely bundled with a later sub-checkpoint's walkthrough rather than per-task.
  - Registration + sign-in screens DONE (2026-07-13): RegisterScreen/SignInScreen, wired into GuestGatePrompt's sign-in/register buttons (T043's placeholder navigation is now real) and new `/register`/`/sign-in` routes. `IdentityAuthService` (core/auth/) drives ADR-0003's cookie-then-PKCE flow. **Found and fixed a real bug verifying this against a browser-realistic setup**: `dio_cookie_manager`'s `CookieManager` explicitly asserts against Flutter Web use (cookies are the browser's job there, not JS-readable/settable) — the app would have crashed on web specifically, the ONLY target this sandbox can build/serve. Fixed with a platform branch (`kIsWeb`): web sets `extra['withCredentials'] = true` (read by dio_web_adapter, needs the API's CORS policy to `AllowCredentials()` — that was ALSO missing and is now fixed in `CorsExtensions.cs`, verified via a live curl-based register→cookie→authorize→token round trip against the docker-compose stack with a real cross-origin `Origin` header); native platforms keep the CookieJar/CookieManager approach (no browser involved there, so nothing handles cookies automatically). MFA, recovery, session list, preferences, and lifecycle screens land with their own sub-checkpoints.
- [X] T151 [US2] Inbox read + channel-preference endpoints (gap review 2026-07-11: listed in contracts/api-v1-surface.md, previously unscheduled): POST /inbox/{id}/read and GET/PUT /me/channels over T023's existing InboxMessage.ReadAt column and ChannelPreference table; wire the pre-authored path definitions from backend/contracts/openapi/paths/notifications.yaml (inboxRead, meChannels) into openapi.yaml and regenerate the Dart client. Placed here because this is where the endpoints become meaningful: T146's real email adapter + T050's transactional mail give users something to opt into, and T052's deactivation warnings produce the first inbox messages to mark read, in backend/src/Modules/Notifications/Infrastructure/
  - Done (2026-07-14): `InboxReadEndpoint.cs` (`POST /inbox/{id}/read`) — idempotent via `message.ReadAt ??= clock.UtcNow`, 404 for unowned/nonexistent messages (indistinguishable, matching the OpenAPI doc's own no-enumeration wording). `ChannelPreferencesEndpoint.cs` (`GET`/`PUT /me/channels`) — both channels (`Push`, `Email`) default opted-out per FR-040a's "opt-in" wording; `GET` lazily materializes a real persisted row for any channel the caller has never touched (so `updatedAt` is always a genuine timestamp, not fabricated per-read), rather than computing defaults purely in-memory. `PUT` rejects an unrecognized channel name with 400 rather than silently ignoring it. Both endpoints wired into `NotificationsModule.MapEndpoints`; the pre-authored `inboxRead`/`meChannels` paths in `notifications.yaml` are now `$ref`'d from `openapi.yaml` (their "pre-authored, not yet wired" header comment removed). New `tests/Acceptance/Features/Notifications.feature` (5 scenarios: default-opted-out, opt into email, reject unrecognized channel, mark-read, idempotent re-mark-read) with its own `NotificationsSteps.cs` — distinct step vocabulary and its own register+PKCE-token helper, per the established per-file convention, rather than reaching into `US02IdentitySteps`' private state (Reqnroll step bindings resolve across the whole assembly, so reusing "Given a registered adult user" verbatim would have silently called a step method with no access to this file's own fields). Inbox-message preconditions seeded via direct `NotificationsDbContext` insertion (this task tests its own read/preference endpoints, not T052's already-covered event-to-inbox pipeline). New Contract 401 tests for all three new operations. Dart client regenerated (also picked up T051-T053's `Session`/`SessionList`/`MeExport` schemas, which had been generated-but-stale since those tasks landed — not previously regenerated). Full regression: Unit 35/35, Integration 1/1, Contract 35/35 (+3), Acceptance 41/41 (+5 Notifications scenarios); frontend `flutter analyze` clean, `flutter test` 46/46 (client regen didn't break anything, no Flutter UI consumes these endpoints yet — that's T055 scope, not this task's).

**Checkpoint**: Both P1 stories complete — guest MVP plus full accounts

---

## Phase 5: User Story 3 — Author a Personal Library (P2)

**Goal**: Private recipes/ingredients incl. house-made; venues with bar libraries

**Independent Test**: Create private recipe + house-made ingredient, use in recipe, verify privacy; create venues and bar-library scoping with switcher

- [ ] T056 [P] [US3] Acceptance tests (failing first) for US3 scenarios 1–4 in backend/tests/Acceptance/Features/US03_AuthorLibrary.feature
- [ ] T057 [P] [US3] Flutter integration test for authoring flows in frontend/app/integration_test/us03_author_test.dart
- [ ] T058 [US3] Author CRUD services + endpoints for recipes (POST/PUT/DELETE /recipes) with library scoping (personal|bar) and privacy enforcement in backend/src/Modules/Catalog/Application/Authoring/
- [ ] T059 [US3] Author CRUD for private ingredients incl. house-made extension (defining recipe, yield, shelf life, storage; circular-reference rejection) in backend/src/Modules/Ingredients/Application/Authoring/
- [ ] T060 [P] [US3] Author CRUD for private equipment (same privacy model) in backend/src/Modules/Equipment/Application/Authoring/
- [ ] T061 [P] [US3] Venues module: Venue entity (PostGIS point, external refs), multi-venue ownership, CRUD /venues + OpenAPI paths in backend/src/Modules/Venues/
- [ ] T062 [US3] Media attachment upload flow for author content (pre-signed URLs, gallery ordering) in backend/src/Modules/Media/Application/Attach/
- [ ] T063 [US3] Flutter library feature: recipe editor (ingredient lines with hierarchy picker, roles, scaling rules), house-made ingredient editor, venue switcher + bar library in frontend/app/lib/features/library/

**Checkpoint**: Authoring loop complete and private

---

## Phase 6: User Story 4 — Track Inventory and "What Can I Make?" (P2)

**Goal**: Inventory at any hierarchy level via photo/barcode/manual; makeability + near-misses

**Independent Test**: All three entry paths; hierarchy satisfaction (Beefeater ⊨ London dry gin); near-miss with substitutions + suitability caveats

- [ ] T064 [P] [US4] Acceptance tests (failing first) for US4 scenarios 1–4 plus the empty-inventory edge case (zero-ingredient recipes only; near-miss thresholds don't list everything) in backend/tests/Acceptance/Features/US04_Inventory.feature
- [ ] T065 [P] [US4] Flutter integration test for inventory + makeability in frontend/app/integration_test/us04_inventory_test.dart
- [ ] T066 [US4] Inventory module: InventoryItem entity + CRUD endpoints /inventory/items with product/class level and optional quantity in backend/src/Modules/Inventory/
- [ ] T067 [US4] Makeability engine: hierarchy + curated/implied substitution resolution, near-miss detection with suitability-caveat surfacing, GET /inventory/makeable in backend/src/Modules/Inventory/Application/Makeability/
- [ ] T068 [P] [US4] AI module foundation: ILlmProviderAdapter/IVisionProviderAdapter ports, provider config, prompt registry persisted in the `ai` schema (version ↔ model ↔ provider ↔ eval results as relational data; prompt template text lives in the repo for code review), per-feature toggles + labeling in backend/src/Modules/Ai/
- [ ] T069 [US4] Bottle recognition: POST /inventory/recognize via ILabelRecognitionPort with pre-filled-manual-form degradation; on-device barcode scan path in backend/src/Modules/Inventory/Application/Recognition/ and frontend/app/lib/features/inventory/scan/
- [ ] T070 [US4] Flutter inventory feature: item list, add flows (photo/barcode/manual), makeable + near-miss views in frontend/app/lib/features/inventory/
- [ ] T147 [P] [US4] Recognition evaluation infrastructure (SC-008; constitutionally implied by prompt-version ↔ eval-results binding): curated labeled bottle-photo dataset including hard cases (glare, partial labels, non-English labels, craft bottles), accuracy harness runnable per prompt-version/provider pair, results recorded against the prompt registry, in backend/tests/Evaluation/Recognition/
- [ ] T148 [US4] Complete the makeable-from-inventory search facet and enable the deferred US1-scenario-5 acceptance assertions (tagged to this checkpoint) in backend/src/Modules/Search/Application/Facets/. Includes full facet-aware pagination, replacing T155's documented post-filter-after-pagination limitation on `/search` (a page can return fewer than `limit` items when the `uses:<ingredient>` facet post-filters) — gap review 2026-07-14, /speckit-analyze C3.

**Checkpoint**: Daily-engagement killer feature live

---

## Phase 7: User Story 5 — Scale, Batch, and Cost Recipes (P2)

**Goal**: Unit conversion, per-line scaling rules, dilution-aware batching, pour costing

**Independent Test**: Scale citrus+egg recipe to 20 (batched vs at-service separation), dilution math matches convention table exactly, pour cost with house-made roll-up

- [ ] T071 [P] [US5] Acceptance tests (failing first) for US5 scenarios 1–4 incl. SC-005 exact-convention assertions in backend/tests/Acceptance/Features/US05_MeasureBatchCost.feature
- [ ] T072 [P] [US5] Flutter integration test for scaling/batching/costing in frontend/app/integration_test/us05_measure_test.dart
- [ ] T073 [US5] Scaling service honoring per-line rules (linear/stepwise/omit/add-fresh) with batched vs at-service separation, GET /recipes/{id}/scale in backend/src/Modules/Measurements/Application/Scaling/
- [ ] T074 [US5] Batch calculator (pre-diluted vs not, target dilution/ABV, keg/bottle/carbonation guidance, container yields, total standard drinks per batch and per container plus per-serving strength using jurisdiction-aware conventions — FR-034/FR-068), BatchResult persistence, POST /recipes/{id}/batch in backend/src/Modules/Measurements/Application/Batching/. Also carries FR-067's responsible-consumption message placement on batch/scaling outputs (T150 built the message/support-resource endpoints already; T150's own note deferred the batch-output placement here since no batch output surface existed yet in US1) — BatchResult's API response must include what T076's UI needs to render the persistent message alongside the batch numbers.
- [ ] T075 [US5] Costing service: IngredientPrice storage, per-drink cost, breakdown, pour-cost %, suggested price, house-made recursive roll-up, GET /recipes/{id}/costing + PUT /me/ingredient-prices/{id} in backend/src/Modules/Measurements/Application/Costing/
- [ ] T076 [US5] Flutter measure feature: scale/batch/cost screens with unit-preference rendering in frontend/app/lib/features/measure/. The batch screen MUST render FR-067's persistent responsible-consumption message (same `ResponsibleUseBanner` pattern already used on `RecipeDetailScreen`/`AboutScreen` — T150) alongside T074's total-standard-drinks/per-serving-strength output; this is the surface T150's own note flagged as pending (no batch output UI existed at Phase 3) — don't let it fall through a second time (gap review 2026-07-14, /speckit-analyze C1).

**Checkpoint**: Professional measurement intelligence complete

---

## Phase 8: User Story 16 — Administer the Platform (Staff) (P2)

**Goal**: Role catalog as config, staff consoles, bootstrap/break-glass, mandatory MFA, audit everywhere

**Independent Test**: Bootstrap Super Admin, approval-gated Curator grant, exercise every console, verify audit entries + MFA enforcement + web-only routes

- [ ] T077 [P] [US16] Acceptance tests (failing first) for US16 scenarios 1–6 in backend/tests/Acceptance/Features/US16_PlatformAdmin.feature
- [ ] T078 [P] [US16] Flutter (web) integration test for admin consoles in frontend/app/integration_test/us16_admin_test.dart
- [ ] T079 [US16] Role catalog config endpoints GET/PUT /admin/roles + platform-scope grant flow with Super Admin approval step in backend/src/Modules/Authorization/Application/Admin/
- [ ] T080 [US16] Mandatory-MFA enforcement at token issuance for platform-role holders in backend/src/Modules/Identity/Application/StaffMfa/. **Must cover pre-existing grants, not only new ones** (gap review 2026-07-14, /speckit-analyze A1): the bootstrap Super Admin (T025, Phase 2) has existed since before MFA enforcement exists, holding a platform-scoped role with no enrolled MFA — SC-016's "zero staff accounts operate without MFA" is only honest at the T140 sweep if this task's enforcement check runs against every RoleGrant already in the table at deploy time, not just grants issued after T080 lands. Add the acceptance test: a platform-scoped user (the bootstrap Super Admin or any RoleGrant holder) without an enrolled/enabled MfaEnrollment is denied staff-console actions once T080 is live, regardless of when the grant itself was created.
- [ ] T081 [P] [US16] Curation console endpoints: /admin/{recipes,ingredients,equipment,glossary,concepts,taxonomy,substitutions,categories,link-overrides} in backend/src/Modules/*/Endpoints/Admin/
- [ ] T082 [P] [US16] Moderation queue: GET /moderation/queue, POST /moderation/actions (unpublish/flag) writing ModerationAction + audit in backend/src/Modules/Community/Application/Moderation/
- [ ] T083 [US16] Account administration: search, suspend/reinstate, fulfill-export/deletion, trigger-reset endpoints (audited; staff see verified age status, never DOB) in backend/src/Modules/Identity/Application/Admin/
- [ ] T084 [US16] Audit-log review endpoint GET /admin/audit-log with filters in backend/src/Modules/Authorization/Endpoints/Admin/
- [ ] T085 [US16] Break-glass runbook (deployment-level re-seed with dual control) in docs/runbooks/break-glass.md
- [ ] T086 [US16] Flutter admin feature area (web-only routes excluded from mobile builds): tier config, role grants, curation, moderation, accounts, audit views in frontend/app/lib/features/admin/

**Checkpoint**: Operator side complete; content workstream unblocked

---

## Phase 9: User Story 6 — Publish, Copy, and Rate Community Recipes (P3)

**Goal**: Publish with consent cascade + copies-survive warning, uniform copy-with-provenance, latest-per-user ratings

**Independent Test**: Publish (cascade + warning), copy from second account with attribution, rate/re-rate, recompute projection equals display, owner + curator unpublish

- [ ] T087 [P] [US6] Acceptance tests (failing first) for US6 scenarios 1–5 + SC-010 recompute equality in backend/tests/Acceptance/Features/US06_Community.feature
- [ ] T088 [P] [US6] Flutter integration test for publish/copy/rate in frontend/app/integration_test/us06_community_test.dart
- [ ] T089 [US6] Publish flow: FR-008a consent-cascade manifest (private ingredients + equipment), FR-008b first-publish copies-survive warning ack, PublicationRecord, POST /recipes/{id}/publish|unpublish in backend/src/Modules/Community/Application/Publishing/
- [ ] T090 [US6] Copy-with-provenance (core + public sources, snapshot semantics, anonymization on source deletion), POST /recipes/{id}/copy in backend/src/Modules/Community/Application/Copying/
- [ ] T091 [US6] RatingEvent append-only store + latest-per-user RatingProjection via outbox, recompute job, POST /ratings + GET /ratings/mine in backend/src/Modules/Community/Application/Ratings/
- [ ] T092 [US6] Concept-page variant attachment (published-only, proposed→approved moderation state), POST /concepts/{id}/variants in backend/src/Modules/Catalog/Application/Variants/
- [ ] T093 [US6] Flutter community feature: publish flow with cascade/warning dialogs, public browse, copy, rate ("your rating" editable) in frontend/app/lib/features/community/
  - Note (2026-07-15, Phase 4 forward-note from John): copy and rate are this app's first *action-type* guest-gated intents (`requireAccount` in `guest_gate.dart`) — until now the only gated action was navigational (T176's account-menu intent, verified in `web_auth_smoke_test.dart`). The preserve-intent contract's real test lands here: after a guest attempts to copy/rate, is prompted, and signs up/in, the ORIGINAL action must actually execute (the copy is created / the rating is submitted), not just that some destination screen loads. Extend the browser-tier smoke set (T179) with a case covering this before calling US6's guest-gate coverage complete.
- [ ] T149 [US6] Complete the rating search facet over RatingProjection and enable the deferred US1-scenario-5 acceptance assertions (tagged to this checkpoint) in backend/src/Modules/Search/Application/Facets/. Includes full facet-aware pagination, replacing T155's documented post-filter-after-pagination limitation on `/search` (same limitation T148 also targets — whichever of T148/T149 lands second should confirm the other's pagination fix already covers this facet too rather than re-solving it) — gap review 2026-07-14, /speckit-analyze C3.

**Checkpoint**: Community loop live with rating integrity

---

## Phase 10: User Story 7 — Consult the Glossary and Encyclopedia (P3)

**Goal**: Reader-facing glossary/article experience with auto-links and cross-discovery

**Independent Test**: Multi-definition term renders once; first-occurrence auto-link + exact return; curator suppress/force; tag cross-discovery

- [ ] T094 [P] [US7] Acceptance tests (failing first) for US7 scenarios 1–4 in backend/tests/Acceptance/Features/US07_Glossary.feature
- [ ] T095 [P] [US7] Flutter integration test for glossary reading + return navigation in frontend/app/integration_test/us07_glossary_test.dart — MUST include the mid-content deep-link case absorbed from T152 (gap review 2026-07-11): from a recipe detail page, tap an auto-linked term inside the instructions/history text, land on the glossary-term detail screen, navigate back, and assert return to the exact originating scroll position (spec §5 / FR-027 "straightforward return navigation to the exact spot the reader left")
- [ ] T096 [US7] Auto-link resolution endpoint GET /glossary/autolink with longest-match and override application in backend/src/Modules/Glossary/Endpoints/ (backend already delivered in T039; this task is now just any US7-specific hardening/coverage on top)
- [ ] T097 [US7] Tag cross-discovery queries ("recipes using X") in backend/src/Modules/Glossary/Application/CrossDiscovery/
- [ ] T098 [US7] Flutter glossary feature: term/article readers, in-text auto-link rendering with return-to-position, related-content rails, in frontend/app/lib/features/glossary/ — this is where T152's deferred work lands (gap review 2026-07-11): a GlossaryTermDetailScreen plus a reusable auto-linked-text widget (calls the T039 GET /glossary/autolink endpoint), wired into EVERY content-text surface FR-027 requires "everywhere," explicitly including the already-built US1 recipe/concept detail screens' instructions/history text (frontend/app/lib/features/discover/recipe_detail/), not only glossary/article bodies. **Presentation guidance (John, 2026-07-12): popover-first** — tapping a glossary term opens a popover showing the definition (reusing T156's entity-popover pattern), with a "full entry" action opening GlossaryTermDetailScreen; popover dismissal satisfies FR-027's return navigation for the common case, and T095's scroll-position acceptance test is RETAINED for the full-screen path

**Checkpoint**: Knowledge base fully navigable

---

## Phase 11: User Story 8 — Reference and Manage Equipment (P3)

**Goal**: Equipment reference with bidirectional recipe/article links + user additions

**Independent Test**: Curated entry displays all fields + links both directions; user-added entry stays private

- [ ] T099 [P] [US8] Acceptance tests (failing first) for US8 scenarios 1–2 in backend/tests/Acceptance/Features/US08_Equipment.feature
- [ ] T100 [US8] Bidirectional link queries (equipment ↔ requiring recipes ↔ articles) in backend/src/Modules/Equipment/Application/Links/
- [ ] T101 [US8] Flutter equipment feature: browse/detail with link navigation, user-entry editor in frontend/app/lib/features/equipment/

**Checkpoint**: Reference triad complete

---

## Phase 12: User Story 9 — Generate Shopping Intelligence (P3)

**Goal**: Lists from recipes/menu+guests/prep; leverage-ranked unlock analysis; export/share

**Independent Test**: Three source paths produce correct quantities; unlock ranking honors hierarchy+substitutions; export leaves the app

- [ ] T102 [P] [US9] Acceptance tests (failing first) for US9 scenarios 1–3 in backend/tests/Acceptance/Features/US09_Shopping.feature
- [ ] T103 [US9] Shopping module: ShoppingList entity and POST /shopping-lists with the recipe-sourced path fully working in this phase; menu- and prep-sourced generation are implemented against the Collections and Prep module contracts here but completed and verified at the Phase 14 and Phase 13 checkpoints respectively (where Menu/PrepList entities exist), in backend/src/Modules/Shopping/
- [ ] T104 [US9] Unlock analysis (recipes-unlocked-per-purchase ranking), GET /shopping/unlock-analysis in backend/src/Modules/Shopping/Application/Unlock/
- [ ] T105 [US9] Export/share (text + document formats via platform share sheet), POST /shopping-lists/{id}/export in backend/src/Modules/Shopping/Application/Export/ and frontend/app/lib/features/shopping/
- [ ] T106 [US9] Flutter shopping feature: list builder, unlock recommendations view in frontend/app/lib/features/shopping/

**Checkpoint**: Purchase loop closed (merchant-of-record boundary respected)

---

## Phase 13: User Story 10 — Manage Prep and Shelf Life (Pro) (P3)

**Goal**: Prep lists with yield math, made-on/shelf-life tracking, expiry notifications, makeability integration

**Independent Test**: Batch + house-made → prep list → complete → clock-advance → inbox warning, drops from makeability

- [ ] T107 [P] [US10] Acceptance tests (failing first) for US10 scenarios 1–3 in backend/tests/Acceptance/Features/US10_Prep.feature
- [ ] T108 [US10] Prep module: PrepList/PrepItem entities (state machine planned→completed→expired, composition/cost snapshot at completion), CRUD + complete endpoints in backend/src/Modules/Prep/
- [ ] T109 [US10] Expiry detection job → PrepExpired/PrepExpiring events → notifications inbox (+ email if opted in; push deferred to Phase 2 per FR-040a) and inventory availability removal in backend/src/Modules/Prep/Application/Expiry/
- [ ] T110 [US10] Flutter prep feature: prep lists, completion flow with made-on date, expiring view in frontend/app/lib/features/prep/

**Checkpoint**: Pro prep workflow live end-to-end

---

## Phase 14: User Story 11 — Collections, Menus, and Print/Export (P3)

**Goal**: Collections; costing-aware menus; artifact-appropriate print/export

**Independent Test**: Collection → priced menu; print/export each artifact type via native pipeline

- [ ] T111 [P] [US11] Acceptance tests (failing first) for US11 scenarios 1–2 in backend/tests/Acceptance/Features/US11_CollectionsMenus.feature
- [ ] T112 [US11] Collections module: Collection entity (+ per-account offline pin flag), Menu promotion with per-item price/description/order, CRUD endpoints in backend/src/Modules/Collections/
- [ ] T113 [US11] Print/export layout service: recipe card (single/batch), spec sheet, styled menu, prep list, shopping list document rendering, GET /print/{artifact}/{id} in backend/src/Modules/Collections/Application/Print/
- [ ] T114 [US11] Flutter collections feature: collection manager, menu editor with pour-cost guidance, native print/share integration in frontend/app/lib/features/collections/
  - Note (2026-07-15, Phase 4 forward-note from John): if a guest can attempt "save to collection" (the wishlist-style action) before the collection exists, it's another action-type guest-gated intent (see T093's identical note) — the preserve-intent contract needs the same "the save actually happens post-signup" verification, not just navigation. Cross-check against whichever of T093/this lands second so the pattern isn't re-discovered independently.

**Checkpoint**: Organization + output workflows complete

---

## Phase 15: User Story 12 — AI-Assisted Features (P4)

**Goal**: Chat, insight blocks, authoring assist, discovery — all toggleable, labeled, gracefully degrading

**Independent Test**: Exercise all with AI on; disable globally/per-feature → zero broken surfaces, deterministic discovery fallback

- [ ] T115 [P] [US12] Acceptance tests (failing first) for US12 scenarios 1–4 + SC-007 all-off sweep in backend/tests/Acceptance/Features/US12_Ai.feature
- [ ] T116 [US12] Chat assistant grounded in platform content, POST /ai/chat with aiGenerated labeling in backend/src/Modules/Ai/Application/Chat/
- [ ] T117 [P] [US12] Contextual insight blocks (page-type adaptive incl. concept variant comparison), GET /ai/insights in backend/src/Modules/Ai/Application/Insights/
- [ ] T118 [P] [US12] Authoring assist with mandatory human-review-before-publish workflow, POST /ai/authoring-assist in backend/src/Modules/Ai/Application/AuthoringAssist/
- [ ] T119 [US12] Discovery: deterministic similarity scorer (flavor/family/shared-ingredient/tasting-log) as fallback + AI-enhanced path, GET /discovery/similar in backend/src/Modules/Ai/Application/Discovery/
- [ ] T120 [US12] Toggle plumbing: /me/ai-toggles + /admin/ai-toggles, outage degradation (circuit breaker → fallback/absence) in backend/src/Modules/Ai/Application/Toggles/
- [ ] T121 [US12] Flutter AI surfaces: chat UI, insight blocks, discovery rail, per-feature toggle settings, clean-absence rendering in frontend/app/lib/features/ai/

**Checkpoint**: AI enriches everything, breaks nothing

---

## Phase 16: User Story 13 — Personal Tasting Log (P4)

**Goal**: Private made-it history feeding discovery, independent of public ratings

**Independent Test**: Log entry privacy; public rating and log rating never interact

- [ ] T122 [P] [US13] Acceptance tests (failing first) for US13 scenarios 1–2 in backend/tests/Acceptance/Features/US13_TastingLog.feature
- [ ] T123 [US13] TastingLog module: TastingLogEntry CRUD endpoints /tasting-log/entries, discovery feed hook in backend/src/Modules/TastingLog/
- [ ] T124 [US13] Flutter tasting log feature: log entry UX from recipe page, history view in frontend/app/lib/features/tasting_log/

**Checkpoint**: Personal journal live

---

## Phase 17: User Story 14 — Bar Mode (P4)

**Goal**: Hands-free high-contrast step-through for recipes and prep lists

**Independent Test**: Step through via touch + voice; screen stays awake; works for prep lists

- [ ] T125 [P] [US14] Flutter integration test (failing first) for bar-mode operation in frontend/app/integration_test/us14_bar_mode_test.dart
- [ ] T126 [US14] Bar-mode UI: one-step-at-a-time view, bar-mode theme, oversized targets, wakelock in frontend/app/lib/features/bar_mode/
- [ ] T127 [US14] Voice advance ("next") via speech-to-text behind accessibility service seam in frontend/app/lib/features/bar_mode/voice/

**Checkpoint**: Wet-hands operation verified on devices

---

## Phase 18: User Story 15 — Configure Offline Access (P4)

**Goal**: Per-device profiles, per-account pins, synced-content intelligence, merge-or-prompt conflicts

**Independent Test**: Each profile tier offline; pinned collections; offline search/makeability; two-device conflict → merge-or-prompt

- [ ] T128 [P] [US15] Acceptance tests (failing first) for US15 scenarios 1–4 (sync API side) in backend/tests/Acceptance/Features/US15_OfflineSync.feature
- [ ] T129 [P] [US15] Flutter integration test for offline profiles + conflict flow in frontend/app/integration_test/us15_offline_test.dart
- [ ] T130 [US15] Sync protocol endpoints: POST /sync/handshake (versioned), GET /sync/changes (cursor, profile-tier + pin filtered), POST /sync/push with conflict responses in backend/src/Api/Sync/
- [ ] T131 [US15] SyncChange log population from ContentChanged events across modules in backend/src/BuildingBlocks/Sync/
- [ ] T132 [US15] Client sync engine: change-log pull, push queue, per-device profile tiers, per-account pinned collections, storage-usage reporting, budget-exceeded handling in frontend/app/lib/data/sync/
- [ ] T133 [US15] Merge-or-prompt conflict resolution UI for user-authored content in frontend/app/lib/features/offline/conflicts/
- [ ] T134 [US15] Offline intelligence: local FTS5 search + local makeability over synced data; online-only feature indicators in frontend/app/lib/data/offline_query/
- [ ] T135 [US15] Offline settings UX: profile picker with storage reporting, pin management in frontend/app/lib/features/offline/

**Checkpoint**: All 16 stories independently functional

---

## Phase 19: Polish & Cross-Cutting Concerns

- [ ] T136 [P] Full launch-content seeding: IBA repertoire + classics, standard equipment, ≥300-term glossary, and the FR-070 responsible-service/responsible-consumption encyclopedia articles (service limits, recognizing intoxication, host duties — auto-linked like all glossary content) (SC-013 content pipeline) in backend/src/Tools/Seeder/Content/
- [ ] T137 [P] Performance validation against SC-012's ratified thresholds (10k sessions × 30 min at 80/15/5 read/search/write mix; p95 ≤ 300/500/600 ms; error rate < 0.1%; no degradation trend; AI endpoints excluded, measured separately with degradation asserted) plus SC-002 search p95, in backend/tests/Performance/
- [ ] T138 [P] Accessibility audit against WCAG 2.1 AA incl. bar mode (automated + manual checklist) in docs/a11y-audit.md
- [ ] T139 [P] Security hardening pass: OWASP checklist, secrets scan, dependency/container scanning gates verified in .github/workflows/ci.yml
  - **Rider ledger (2026-07-17, docs batch)** — every item filed against this task, one place, so a future pass doesn't have to grep for them: (1) **Key custody / Azure Key Vault** — elevated to a launch-gating criterion (John, 2026-07-13; see the bullet immediately below and ADR-0004). (2) **CSP hardening** (T177's ruling, 2026-07-15): web's persisted-refresh-token XSS exposure is explicitly documented and accepted for V1 on the condition that strict CSP is the compensating control — not yet built, filed here as this task's job. (3) **T166** is launch-gated *alongside* this task (paired gating, not a rider on it) — see the cross-reference bullet below.
  - See also **T166** (below): true immediate session/access-token invalidation for high-severity lifecycle events, launch-gated alongside this task per John's 2026-07-14 ruling on the T051/T052 session-revocation gap.
  - **LAUNCH-GATING CRITERION** (elevated from rider, John 2026-07-13 — decision recorded in the new docs/adr/0004-data-protection-key-custody.md, T141): Data Protection key-ring-at-rest encryption. The identity module's key ring (backing BOTH the DOB and MFA-secret ciphers, T162/T164) persists via `PersistKeysToDbContext<IdentityDbContext>` with no `ProtectKeysWith*` — master keys effectively plaintext in the same database as the ciphertexts, so one DB compromise yields both. Ratified direction: an `IXmlEncryptor`-backed key-custody adapter behind a new secrets/KMS port (constitution Principle III) — Azure Key Vault adapter for deployed environments (Azure is the expected landing platform), local-dev fallback explicitly dev-mode (deployment-supplied cert, or unwrapped with a loud startup warning), config-selected. **Acceptance wording**: "master key material is never persisted unwrapped in the same trust domain as the ciphertexts it protects; Production refuses to start otherwise" — i.e. `ASPNETCORE_ENVIRONMENT=Production` MUST fail fast at startup if no real custody adapter is configured, not silently fall back to unwrapped keys. Rotation/key-ID prerequisites are already satisfied (T162 item 3), so the adapter is drop-in once built. Not required before continuing feature work; required before any non-local deployment.
- [ ] T166 [P] True immediate session/access-token invalidation for high-severity events (deactivation, staff suspension, sign-out-everywhere) — launch-gated alongside T139 (John's ruling, 2026-07-14, on the T051/T052 session-revocation gap: self-contained access tokens can't be killed mid-lifetime without a per-request DB check; T052 shortened `AccessTokenLifetime` to 10 minutes as the immediate mitigation, bounding but not eliminating the post-revocation exposure window). Scope: a cached denylist or security-stamp check (distributed cache lookup per request, entries live only as long as the token lifetime so the set stays small) applied ONLY to the high-severity event set above — not a platform-wide reference-token switch, which John explicitly rejected as the wrong cost for what's now a ≤10-minute low-severity window for ordinary session revocation. Purely additive middleware on top of T052's existing revocation calls; nothing in T052 forecloses it.
- [ ] T182 [US2] Sign-out-everywhere action (docs batch U1/FR-001b, 2026-07-17). `POST /me/sessions/sign-out-everywhere` — revokes every one of the caller's active `SessionDevice` rows in one action, reusing `SessionsEndpoints.RevokeAsync`'s own per-session mechanism (`IOpenIddictAuthorizationManager.TryRevokeAsync` + `RevokedAt`) looped over the caller's live sessions, not a new revocation mechanism. Filed near T166 (not merged into it): T166 is about closing the *immediate access-token liveness* gap for high-severity events including this one; T182 is the simpler, already-buildable *action itself* (bulk refresh-token-family revocation), which doesn't need T166's cached-denylist infrastructure to exist first — a caller loses future refresh capability everywhere immediately either way, same ≤10-minute access-token exposure window as any other revoke today. Acceptance: sign in from two+ devices, sign-out-everywhere from one, assert every session's next refresh is rejected and the sessions list is empty. Client: a button on the sessions screen, not yet designed.
  - **See T167** (resolved 2026-07-14): a flake that LOOKED like it might be on this exact revocation path turned out, once root-caused, to be an unrelated test/query ordering bug (a missing deterministic sort tiebreak plus a test index mistake it was masking) — not an OpenIddict revocation race. No evidence surfaced that revocation itself is non-atomic; T166's own rationale (self-contained access tokens outliving revocation) stands on its original grounds, unaffected by this finding either way.
- [X] T167 Flake investigated and ROOT-CAUSED (2026-07-14): T051 scenario 15's "the first device's session can no longer be refreshed" assertion failed intermittently across the session (first: full-suite run right after `c0ab473`; second, decisive: the very first real commit run through T168's new pre-commit hook, escalating per the broadened rule below). **Root cause, confirmed via a temporary diagnostic (queried the raw `SessionDevice` rows, printed timestamps, then removed it)**: NOT an OpenIddict/revocation race at all — the real bug was in the test itself, masked by a genuine secondary bug in the production query. `SessionsEndpoints.ListAsync` ordered sessions by `LastSeenAt DESC` with no tiebreak; `WhenTheUserSignsInFromTwoDevices` drives two token exchanges back-to-back with no `TestClock.Advance()` between them, so both `SessionDevice` rows get a BYTE-FOR-BYTE IDENTICAL `LastSeenAt` (confirmed directly via the diagnostic) — leaving Postgres's own tie-break (unspecified, and observed to vary run to run) to decide the order. `_sessionIds[0]` was therefore sometimes device 0's session and sometimes device 1's, but `ThenTheFirstDevicesSessionCanNoLongerBeRefreshed` (the T052-era addition) always checked `_deviceRefreshTokens[0]`, assuming `_sessionIds[0]` meant device 0 — wrong roughly half the time. **Two fixes, both needed** (the query fix alone would have made the test fail deterministically instead of intermittently — traced through by hand before touching either): (1) `SessionsEndpoints.cs` — added `.ThenByDescending(s => s.Id)` as a secondary sort key. `SessionDevice.Id` is a UUIDv7 (via `IUuidGenerator`), genuinely time-ordered by real wall-clock regardless of `TestClock`, so this makes the ordering fully deterministic AND semantically correct (most-recently-created wins ties) — a real production correctness fix, not just a test workaround, since two real devices connecting within the same clock tick would hit the identical nondeterminism. (2) `US02IdentitySteps.cs` — `ThenTheFirstDevicesSessionCanNoLongerBeRefreshed` now checks `_deviceRefreshTokens[1]`, not `[0]`: with the list newest-first and device 1 signing in after device 0, `_sessionIds[0]` (the one `WhenTheUserRevokesTheFirstSession` actually deletes) is device 1's session, not device 0's — index `1` names the correct device, not an off-by-one. **Verified**: 11 consecutive full-suite/filtered runs post-fix (including 5 in the exact fresh-`Release`-build pattern that reproduced both failures), zero recurrence; all four backend suites green. **Process gap also fixed**: the first occurrence was investigated from only a tail-truncated summary line, not full output — this time, full `dotnet test` output was captured to a scratchpad file before drawing conclusions, which is what made the root cause traceable at all.
  - **Post-T177 re-confirmation (T177 #102, 2026-07-17)**: ADR-0005 required this disposition be re-confirmed once T177 finished rebuilding the exact refresh/rotation/session-identity path T167's flake lived on — reasoning-stands isn't enough on its own here, since the code T167 touched (`SessionsEndpoints.ListAsync`'s ordering) is exactly what T177 kept building on top of. **Evidence**: scenarios 15 (list/revoke, T167's own origin), 18 (reuse detection), and 19 (absolute cap) run together **10/10 consecutive clean runs** (`--filter` targeting all three), zero failures, zero flakes. Folded in per the #100 done-note's own forward-note: the cross-tab mechanism test (`web_multitab_test.dart`) is also revocation-adjacent (it exercises the exact reuse-detection boundary a broken election would trip) and has **8 of its own clean repeated runs already on record** across #100 and #100's item-0 work (4/4 pre-hygiene, 4/4 post-hygiene). Combined: T167's disposition is **CONFIRMED CLOSED post-T177**, not merely carried over from its 2026-07-14 closure.
  - **Escalation rule that fired (John 2026-07-14)**: the broadened rule — any revocation-adjacent intermittency counts as the second occurrence, not only a repeat of the identical assertion — is exactly what triggered this investigation rather than a third silent retry. Recorded here as the rule now having a real precedent, not just a hypothetical.
- [X] T168 Local CI-gate backstop (John's directive, 2026-07-14, on the c0ab473 review: "every CI-enforced guarantee is currently enforced only by session discipline — that's a single point of failure the constitution assumes doesn't exist"). Priority: before Phase 4 sub-checkpoint 4 closes — the walkthrough shouldn't certify a build no pipeline ever checked.
  - Done (2026-07-14): no git remote is configured for this repo at all (checked — `git remote -v` is empty), so `.github/workflows/ci.yml`'s gates have never actually executed against this codebase; every commit so far has depended entirely on manually running the equivalent checks by hand. New `scripts/pre-push-checks.sh` runs the practical-to-run-locally subset of CI's jobs: `backend-build`, all four `backend-tests` suites, `frontend-analyze` (analyze + format check), `frontend-unit-tests`, `client-regen-drift-check`, and the NuGet half of `dependency-scan`. Deliberately excludes `frontend-integration-tests` (needs an Android emulator) and `container-scan` (needs Trivy) — both stay CI-only, noted explicitly in the script's own header rather than silently dropped. New `scripts/install-git-hooks.sh` installs that script as **both** `pre-commit` and `pre-push` (run once, already run for this repo) — deliberately not pre-push-only: with no remote configured, a pre-push hook alone would never fire against this repo's actual workflow (commits land straight on `main`, nothing gets pushed), so pre-commit is the real enforcement point today; pre-push stays wired for free the moment a remote exists. `SKIP_LOCAL_CI_GATE=1` escape hatch for a deliberate one-off skip (not `--no-verify`, which bypasses without a visible reason in the command itself). README.md documents the setup. **Explicitly out of scope, needs John's input, not guessed**: actually provisioning real CI (creating/connecting a GitHub remote, or another CI host) is a repo-hosting decision this task doesn't make — the local hook is the interim John described, not a replacement for eventually wiring the real thing.
  - **Reopened (2026-07-15, John's directive — Phase 4 sign-off amendment)**: status-checked per John's request — confirmed again, `git remote -v` is still empty, `gh auth status` also shows no valid token. `.github/workflows/ci.yml` has **never executed once**, on any commit, since it was written. The local pre-commit/pre-push gate (above) is real and working, but it is session-discipline-dependent in the same way John's original c0ab473 concern described: it only runs on machines that have `scripts/install-git-hooks.sh` run, and nothing enforces that on a clone or a second contributor. **First position, before T177**: get real CI live (requires a hosting decision — GitHub remote under John's account, public or private — this task will not create one unilaterally, matching the git-safety-protocol rule against creating/pushing to shared remotes without explicit authorization) and wire `scripts/run-web-integration-tests.sh` / the `web_auth_smoke_test.dart` browser tier into `ci.yml` as a required check once a remote exists. **Phase 4 sign-off is gated on this being done, not just T177/T178/re-walks.**
  - **Done (2026-07-15)**: John supplied the remote (`https://github.com/jmstamets/specflow.git`, confirmed empty/reachable via `git ls-remote` first). Wired as `origin`, pushed `main` (`ab44980`) — `.github/workflows/ci.yml` executed for the very first time. **First run surfaced 3 genuine, previously-invisible gaps**, each root-caused from real log evidence (not guessed), fixed, and re-verified on a second push (`e171c60`): (1) `dependency-scan` correctly failed on real CVEs — MailKit/MimeKit 4.13.0 both carry known moderate advisories; the local hook had been running the identical check on every commit and never once caught it, root-caused to the check being fail-open (it only greps for a positive "vulnerable packages found" match and treats an inconclusive/failed advisory-feed query the same as "clean" — confirmed by direct reproduction that the exact same command run moments apart could report clean or find the CVEs depending on whether the NuGet advisory query actually completed). Fixed the finding (bumped MailKit/MimeKit to 4.17.0, which required a small real fix in `SmtpEmailChannelAdapter.cs` — a tightened nullable annotation on `AuthenticateAsync` caught a genuine gap where `Password` was never validated non-null alongside `Username`) AND hardened the check itself in both `ci.yml` and `pre-push-checks.sh` to fail closed when the advisory query doesn't demonstrably complete ("The following sources were used" must appear), not just when it demonstrably finds something. (2) `container-scan` failed at "Set up job": the pinned `aquasecurity/trivy-action@0.24.0` ref had never actually existed — confirmed via `git ls-remote` that the real tag is `v0.24.0` (with a `v`), a typo present since the workflow was first written and invisible until CI actually ran. Re-pinned to the current release, `v0.36.0`. (3) `frontend-integration-tests` (pre-existing Android-emulator job, not part of this task's original scope) hung 1h+ with no timeout and had to be cancelled by hand — `ubuntu-latest` lacks reliable KVM acceleration for the emulator. Added `timeout-minutes: 20` so it now fails fast and visibly (confirmed: failed cleanly at 16m39s on the verification run) instead of hanging silently for hours; root-causing/fixing the emulator itself is out of scope here, filed as **T180**. **The actual mandate — wiring the browser tier in as a required check — delivered**: new `frontend-web-browser-tests` CI job runs `scripts/run-web-integration-tests.sh` (the real headless-Chrome `web_auth_smoke_test.dart` suite — the tier that caught T169/T175/T176, every green-but-dead bug found this project) against a real `docker compose up` stack, no KVM needed since Chrome ships on `ubuntu-latest`. **Verification run (`29467554947`) confirmed**: backend-build, all 4 backend-tests, frontend-analyze, frontend-unit-tests, client-regen-drift-check, dependency-scan, container-scan, and `frontend-web-browser-tests` all green; only the pre-existing (now T180-tracked, non-blocking-by-design) Android job red. Real CI is live, on every push to `main`, and the browser tier is in it. **Phase 4 sign-off's CI condition is met.** Note: GitHub branch-protection "required status checks" (blocking PR merges specifically) is a separate repo-settings action not yet configured — not needed today (no PR workflow yet, commits land straight on `main`), flagged here so it isn't silently assumed done.
  - **Four riders closed (2026-07-16, John)**, in order:
    1. **Branch protection** — `main` now requires all 11 real checks (`backend-build`, 4× `backend-tests`, `frontend-analyze`, `frontend-unit-tests`, `client-regen-drift-check`, `dependency-scan`, `container-scan`, `frontend-web-browser-tests`) via `PUT /repos/.../branches/main/protection`, `strict: true`, `enforce_admins: true`, no force-push/deletion. `required_pull_request_reviews` deliberately left null (solo dev — self-approval is theater; John clicking merge IS the review). `frontend-integration-tests` deliberately excluded (rider 2). **Correction to an earlier assumption**: `required_status_checks` blocks *direct pushes* to `main`, not merely PR merges — verified the hard way when the very push carrying these changes was rejected (`GH006: 11 of 11 required status checks are expected`), self-locking `main` since checks only trigger *on* push. **Resolution (John, 2026-07-16): adopt a lightweight PR workflow** — every change to `main` now goes through a branch + PR that must go green before John merges (first exercised as PR #1, merged `01bf5cc`). The full merge-model ruling (status-checks-only, Claude opens PRs but never self-merges — the CC classifier blocks agent self-merge and that's correct — John merges; bot/GitHub-App identity deferred as more ceremony than a solo dev needs) is recorded in memory `feedback-solo-dev-merge-model` so future sessions don't re-derive it.
    2. **Android job demoted** — `frontend-integration-tests` gets `continue-on-error: true` (on top of rider-1's 20-min timeout) so a red, KVM-starved emulator job can no longer turn the whole run red-by-design; its own steps still show true pass/fail.
    3. **Fail-open sweep, full verdict** (audited every check in `ci.yml` + `pre-push-checks.sh`, evidence-based not assumed):
       - `backend-build`: fail-closed by nature (build success/failure, no inconclusive state). N/A.
       - `backend-tests` (4 suites): confirmed via direct reproduction that `dotnet test --filter <no-match>` exits **0** — a real fail-open mode. Not currently reachable (grepped both files: no `--filter` used anywhere), but hardened anyway for defense-in-depth: both `ci.yml` and `pre-push-checks.sh` now `tee` the output and require `Passed!.*Total: [1-9]` to appear, failing loudly otherwise. Verified: still green, Contract suite's new count (36) correctly matched.
       - `frontend-analyze`: fail-closed by nature (analyzer/formatter are deterministic, always run). N/A.
       - `frontend-unit-tests` (`flutter test`): tested directly — a zero-test target exits **79** (nonzero). Fail-closed already, confirmed empirically, no hardening needed.
       - `frontend-web-browser-tests` (`flutter drive`): tested directly — a zero-`testWidgets()` target prints "All tests passed." and exits **0**. **Real, confirmed fail-open finding**, same shape as the dependency-scan one from T168's first pass. Fixed in `web_auth_smoke_test.dart`: a module-level `executedCaseCount`, incremented at the start of each of the 4 cases, asserted `== 4` in `tearDownAll` — an accidentally-emptied or silently-skipped smoke tier now fails for real. Verified: reran the full suite post-fix, still green (4/4 executed).
       - Docker health-wait (same job): already fail-closed by construction — reviewed the step: `docker compose up -d --build` fails the step on any build/start error; the health-poll loop explicitly `exit 1`s with `::error::` (plus a `docker compose logs` dump) if the API never becomes healthy within 120s. No silent "stack never came up, tests just didn't run" path exists. N/A, no change needed.
       - `client-regen-drift-check`: fail-closed by construction — read `generate-client.sh` line by line: `set -euo pipefail`, no `|| true` anywhere in the chain (including inside its `( cd ...; dart pub get; dart run build_runner ... )` subshell), so any regeneration failure aborts before `check-client-drift.sh`'s `git diff` check is ever reached — a broken regen can't be misread as "no drift." N/A.
       - `dependency-scan`: already hardened in T168's first pass (this session) — the "sources were used" fail-closed check.
       - `container-scan` (Trivy): fail-closed by the tool's own design (its `exit-code` input only governs the "vulnerabilities found ≥ threshold" case; trivy exits nonzero unconditionally on execution errors like a failed DB pull, per its documented behavior) — backed by real evidence, not just the docs: this run's log shows a genuine DB-backed multi-target scan (`ubuntu 24.04` + several `dotnet-core` deps.json targets, real "Detecting vulnerabilities..." lines, a real Report Summary table), not a stub. N/A.
       - "Coverage gate": **doesn't exist anywhere in this repo** — grepped for it explicitly, zero hits. N/A (not a real check to harden).
    4. **SmtpEmailChannelAdapter password-validation test**: none existed — the one prior contract test (`SmtpEmailChannelAdapterContractTests`) never configures `Username`/`Password`, so the authenticated branch (and the new `ArgumentException.ThrowIfNullOrWhiteSpace(settings.Password)` guard) was completely untested. Added `SendAsync_rejects_a_configured_username_with_no_password` to the same file, reusing its real smtp4dev Testcontainer (Username set, Password null → asserts `ArgumentException` before any auth attempt). Verified passing (Contract 35→36).
  - **Repo renamed specflow → specpour (2026-07-15, John)**: post-rename sweep — `grep -rn specflow` across the tree found only this task's own historical note (accurate as written; the remote genuinely was `.../specflow.git` at the moment T168 was done, kept verbatim) and unrelated hits in `research.md` (the Reqnroll/SpecFlow *testing-library* name, a coincidental match, not this repo). No badge URLs, no registry/image-name references, no workflow hardcoding the repo name (`.github/workflows/*.yml` has zero `jmstamets`/`ghcr.io`/`github.repository` references — nothing needed updating because nothing was ever repo-name-coupled) — confirmed via direct grep, not assumed. No container images were ever pushed to any registry under either name (`container-scan` only does a local `docker build`, never a push), so the "ghcr packages don't redirect" risk doesn't apply here. Local `origin` updated to `https://github.com/jmstamets/specpour.git` and verified reachable (`git ls-remote`); confirmed GitHub's rename preserved all Actions history (`29467554947`/`29463958950` both resolve under the new name). Fresh post-rename push (`<pending>`) re-verified green with all the same required checks intact.
- [X] T169 [US2] Web registration/sign-in PKCE fix + browser-context test (Phase 4 walkthrough finding #1, John 2026-07-15). **Root cause**: registration was green in every suite but dead in a real browser. Three layered issues, diagnosed from correlated server logs + direct reproduction: (a) 403/400 were correct backend behavior — 403 = underage gate (John tested an underage DOB; the age gate works), 400 = `UserManager.CreateAsync` "Passwords must be at least 12 characters" (short-password attempt; drives T171). Registration itself 201s. (b) The real break: after the 201, the client's ADR-0003 PKCE step (`GET /connect/authorize` → 302) redirected the browser to a dead `http://localhost:5173/callback` (a stale Vite-default redirect URI, hardcoded in both `identity_auth_service.dart` and `OpenIddictClientSeedingHostedService`'s default), → `ERR_CONNECTION_REFUSED` → the raw DioException the form showed. But swapping the port alone wouldn't fix it: **on Flutter web the browser follows the 302 opaquely and Dio's web adapter discards the redirected URL** (verified from `dio_web_adapter` 2.2.0 source — `Response.realUri` stays the original request URL), so the "read the Location header" mechanism is structurally impossible in a browser (it only ever worked in the C# acceptance test and on native, which honor `followRedirects:false`). (c) The code flow after registration is intended (ADR-0003); only its web mechanism was broken. **Fix (surgical, empirically chosen — John: "verify surgical, else redirect-flow")**: a new same-origin no-op landing endpoint `GET /connect/spa-callback` (TokenEndpoints) is the web client's registered redirect_uri; on web the client uses `fetch` (`web_authorize.dart`, conditional-imported so native still compiles) to follow the redirect and read the code straight off `Response.url` — proven readable cross-origin-with-credentials in a headless-Chrome probe before implementing. Native keeps the custom-scheme + Location-header path unchanged. Redirect URI is now runtime-derived (web) / config-defaulted (`Identity:OAuthClient:RedirectUris`), never hardcoded to a served origin; the seeder now **syncs** redirect URIs from config on every boot (was create-once, which is why the stale `:5173` persisted). **The missing test**: `integration_test/web_registration_test.dart` + `test_driver/integration_test.dart` + `scripts/run-web-integration-tests.sh` — a real headless-Chrome `flutter drive` run against the live docker stack that exercises the web-only PKCE path unit/widget tests can't. Verified it FAILS on the pre-fix code (`/connect/authorize did not redirect (status 200)`) and PASSES with the fix — the exact green-but-dead guard John asked for. Building it also surfaced + fixed a latent `find.byType(TextField).first` bug in `us02_identity_test.dart`'s date-picker entry (typed into the email field behind the dialog; `.last` is correct — only ever visible in a real browser). Also fixed the acceptance suite's `RedirectUri` const (`:5173` → spa-callback, now that `:5173` is unregistered). **Also surfaced (environment, not code)**: the running Postgres volume was behind on T050–T163 migrations — rebuilt+ran migration-runner. Regression: backend Unit 35 / Integration 1 / Contract 35 / Acceptance 41; frontend analyze clean, `flutter test` 58/58, `flutter build web` served+checksum-verified at :8080; browser drive test green. Raw-error presentation (the DioException showing verbatim) is T170; password UX is T171.
- [X] T170 [US2] Global API-error presentation layer (Phase 4 walkthrough finding #2, John 2026-07-15 — "New task"). Raw exceptions must never render to users (the walkthrough showed Dio's internal `[connection error]` text verbatim in the register form). Map ProblemDetails/validation responses to friendly, field-level messages; unexpected failures get a generic message plus a **correlation ID surfaced in the UI** (and logged) so walkthrough/support reports can be matched to server logs (the backend already emits `correlationId` in its problem+json — see the reproduced 400/403 bodies). Acceptance test: a forced 500 renders the friendly form, never exception text. Front-end presentation concern spanning `describeIdentityError` (currently returns `error.toString()` for non-problem-json) and every screen's catch block.
  - **Done (2026-07-17)**: `identity_auth_service.dart`'s `describeIdentityError` (the single funnel all 12 identity screens' catch blocks already go through — zero call-site changes needed) rebuilt on a new `describeApiError`, which classifies every failure explicitly rather than falling back to `error.toString()`: **no response at all** (network/connection error — the walkthrough's own `[connection error]` finding) → a generic "check your connection" message, no correlation ID (never had a server round trip to get one from). **5xx** → always the generic server-error message regardless of whether the body happens to be parseable, because Development's `UseDeveloperExceptionPage` returns a raw HTML page (not even JSON) and nothing guarantees a future custom exception handler stays safe to show verbatim — correlation ID attached when the body carried one. **4xx** → the existing friendly `detail`/`title` extraction (unchanged behavior), plus defensive support for ASP.NET Core's `errors: {field: [messages]}` ValidationProblemDetails shape (not populated by any endpoint today — every identity validation failure currently joins into one `detail` string — but this funnel handles it if a future endpoint ever returns it, since it's the shared entry point). Correlation ID is deliberately NOT shown for 4xx per this task's own scoping ("unexpected failures get... a correlation ID" — routine validation errors don't need one). New `ApiErrorPresentation` (message + optional correlationId) is the richer primitive `describeApiError` returns; `describeIdentityError` stays a string-returning wrapper for backward compatibility, appending the correlation ID inline for now (a selectable/copyable presentation with its own copy button is T172's job, which reads the same correlationId field).
  - **Acceptance test**: `test/core/auth/identity_auth_service_test.dart` (7 cases) — a 500 with a JSON body containing exception-looking text asserts that text never reaches `result.message`; a 500 with a genuinely unparseable HTML body (matching Development's real `UseDeveloperExceptionPage` output) also gets the generic message; a connection error with no response never surfaces Dio's internal type/message strings; a non-`DioException` unexpected error also gets the generic message; 4xx `detail`/`title` extraction is unchanged and carries no correlation ID; field-level `errors` are joined when present. Tested the pure function directly (matching the `TokenRefreshInterceptor` precedent) rather than only incidentally through a widget test, since it's provably the single funnel every consuming screen already shares.
  - **Verified**: `flutter analyze` clean, `flutter test` 65/65 (+7, no regressions).
- [X] T171 [US2] Registration password UX (Phase 4 walkthrough finding #3, John 2026-07-15 — "New task"). Single obscured field today with no reveal toggle and no policy guidance — a typo is undetectable and the 12-char minimum is discovered only by a 400. Deliver: reveal/hide toggle (which is why no confirm field is needed — reveal replaces it per current UX practice); inline requirements shown before first failure; validation errors bound to the field, not a form-level exception. **Actual server password policy to mirror client-side** (enumerated 2026-07-15 from IdentityModule's `AddIdentityCore` options + the reproduced 400): minimum length 12; NO other composition requirements (RequireDigit/Lowercase/Uppercase/NonAlphanumeric all disabled per T047's NIST-800-63B relaxation) — so the only client-side rule to show/enforce is "at least 12 characters." Keep client and server in lockstep so they can't drift.
  - **Done (2026-07-17)**: `register_screen.dart`'s password field gained (1) a reveal/hide `IconButton` suffix toggling `obscureText`, no separate confirm field (reveal replaces it, per the task's own instruction); (2) `helperText` showing "At least 12 characters" unconditionally, not only after a failure — mirrors `IdentityModule`'s actual `RequiredLength = 12` exactly, via a shared `_minimumPasswordLength` constant with a doc comment pointing at the server config so the two can't silently drift; (3) a local pre-submit length check that rejects a too-short password with ZERO network round trip, binding the failure to the field's own `errorText` rather than the form-level `_errorMessage` banner; (4) server-side password errors (e.g. a real "Passwords must be at least 12 characters." 400, which the backend still joins into one `detail` string, not a field-keyed shape — see T170's `describeApiError`) are ALSO routed to the field, via a text-match heuristic (`message.toLowerCase().contains('password')`) — documented as a heuristic, not a structural guarantee, since the backend doesn't return field-keyed validation errors anywhere in this codebase today. New l10n keys: `registerPasswordPolicyHint`, `registerPasswordRevealTooltip`, `registerPasswordHideTooltip`.
  - **Verified**: `flutter analyze` clean, `flutter test` 68/68 (+3: toggle actually flips `obscureText`; hint visible before any submit attempt; a too-short password is rejected with `registerCallCount == 0`, proving no network call happened), existing register/sign-in tests unregressed.
- [X] T172 [US2] Selectable/copyable error + content text (Phase 4 walkthrough finding #4, John 2026-07-15). Flutter web's canvas rendering makes error messages uncopyable. Wrap error surfaces (and ideally recipe/content surfaces) in `SelectionArea`/`SelectableText`, and give the error component a copy button including the correlation ID from T170 (so a user can copy-paste the exact failure + ID into a support/walkthrough report). Pairs with T170.
  - **Done (2026-07-17)**: new shared `core/widgets/api_error_display.dart` (`ApiErrorDisplay`) — wraps the error message in `SelectionArea` (genuine drag-to-select, matching real web text) plus an explicit copy `IconButton` (more discoverable/reliable than drag-selection alone, especially on touch web) that copies the exact text shown, which already carries the correlation ID inline when one is present (T170's `describeIdentityError`). Batch-refactored all **12** identity screens that render an error message onto this ONE shared component (a scripted sweep — same file/key list every screen already used, zero call-site key changes, so every existing `find.byKey`/`find.text` test assertion kept working unchanged): `register_screen.dart`, `sign_in_screen.dart`, `mfa_challenge_screen.dart`, `mfa_settings_screen.dart`, `recovery_request_screen.dart`, `recovery_confirm_screen.dart`, `account_data_screen.dart`, `account_lifecycle_screen.dart`, `sessions_screen.dart`, `channel_preferences_screen.dart`, `complete_external_registration_screen.dart`, `external_sign_in_callback_screen.dart`. Recipe/content-surface selectability ("ideally") not pursued — no rich content screen exists yet in this V1 slice to apply it to.
  - **A real test-framework gotcha found and fixed while writing the acceptance test, not guessed**: `Clipboard.getData` awaited directly inside a `testWidgets` body hangs indefinitely (confirmed by direct minimal reproduction, isolated from this widget entirely — a plain `ElevatedButton` + `Clipboard.setData`/`getData` alone reproduced the same 20s+ hang) — a real async platform-channel round trip doesn't resolve inside `testWidgets`' fake-async test zone without help. Fixed with the established Flutter pattern: mock `SystemChannels.platform` directly (`TestDefaultBinaryMessengerBinding...setMockMethodCallHandler`) so `Clipboard.setData`/`getData` resolve synchronously within the test zone — not specific to this widget, a general fact about testing `Clipboard` in Flutter.
  - **Verified**: new `test/core/widgets/api_error_display_test.dart` (3 cases: message renders with its key, `SelectionArea` present, copy button copies the exact displayed text to the clipboard). `flutter analyze` clean, `flutter test` 71/71 (+3, no regressions across the 12-file refactor).
- [X] T173 [US2] Social sign-in productionization (Phase 4 walkthrough finding #5, John 2026-07-15 — "New task"; supersedes/absorbs T165's fake-handler harness intent for the config-rendering slice). **Report on what T049 actually delivered and where the chain breaks** before building. Deliver: config-driven provider rendering (unconfigured providers don't render — no dead buttons; widget test included); a credentials-setup doc for Google/Microsoft (John registers the apps; openid/email/profile scopes only); and record the **App Store Review Guideline 4.8 constraint** (Sign in with Apple required on iOS if other social logins are offered there). The DOB-completion design for brand-new social accounts stands as specified (John's positive finding) — untestable until real credentials exist.
  - **Report (2026-07-17)**: `IdentityModule.RegisterServices` already registers each of Google/Microsoft/Apple's `IExternalIdentityProviderPort` only when that provider's `ClientId` is configured (T049) — the backend half of "don't offer what isn't configured" already existed. The gap was purely frontend: `SocialSignInButtons` rendered all three unconditionally regardless of what the backend actually supports, so an unconfigured provider (every environment today — no real credentials exist anywhere) was a dead button that would 400 "Unknown provider" on tap.
  - **Done (2026-07-17)**: new anonymous `GET /auth/external/providers` (sibling route to the existing `/auth/external/{provider}` family, same file — considered and rejected the Authorization module's `/me/entitlements` as a home, since that's a capability/tier concern, not an auth-methods one) returns `{"providers": [...]}`, names only, sourced by DI-enumerating the registered `IExternalIdentityProviderPort`s — never secrets. OpenAPI (`identity.yaml` + root `openapi.yaml`) authored first per Principle II, Dart client regenerated (`scripts/generate-client.sh`, zero drift). `SocialSignInButtons` now fetches this once per screen mount (`configuredExternalProvidersProvider`, `FutureProvider.autoDispose`) and renders a provider's button only if its key is present; zero configured providers omits the whole section, including the "or" divider, rather than a loading/error flash of dead buttons. New `docs/oauth-provider-setup.md`: Google (Cloud Console) + Microsoft (Entra ID) app-registration steps, exact local-dev redirect URIs (`https://localhost:7131/api/v1/auth/external/{provider}/callback`), the real config keys (`ExternalProviders:{Provider}:ClientId/ClientSecret`, Apple's `TeamId`/`KeyId`/`PrivateKey` in place of a static secret), `openid email profile` scopes only (no birthdate scope, per the DOB-completion design), and the **App Store Review Guideline 4.8** note (Sign in with Apple required on iOS if other social logins ship there — forward note, iOS hasn't shipped).
  - **Boundary with T165 (D1, 2026-07-17)**: recorded on T165's own entry above — T173 absorbed only the config-rendering slice; T165's fake-OAuth-handler harness (exercising `CallbackAsync`'s real branches end-to-end) remains its own, un-absorbed, deferred scope.
  - **A real regression found and fixed via reproduction, not guessed**: two pre-existing widget tests (`external_sign_in_widget_test.dart`) pumped `SignInScreen`/`RegisterScreen` with a bare `ProviderScope` and no Dio override, so once buttons became conditional on a real network call, those tests' unconditioned `findsOneWidget` assertions broke (confirmed by direct `flutter test` run, not assumed). Replaced with five cases against a fake `identityApiProvider` override driving `GET /auth/external/providers`: three-providers (sign-in and register screens), one-provider (button count + divider present), zero-providers (nothing renders, divider included) — satisfying this task's own "widget test included" requirement properly rather than leaving the old unconditioned tests in place.
  - **Verified**: `flutter analyze` clean; `flutter test` 77/77 (full suite, +5/-2 net vs. pre-T173's 74); backend `dotnet test` all four suites green (Acceptance 46/46, Contract 37/37 incl. new `GetAuthExternalProviders_conforms_to_its_schema_and_is_empty_when_none_are_configured`, Integration 1/1, Unit 35/35); `scripts/check-client-drift.sh` clean (zero regen drift); NuGet vulnerability scan clean. Not tested: the actual OAuth handshake — no real provider credentials exist in any environment this codebase has run in, matching this task's own scope boundary and T165's remaining deferred scope.
- [X] T174 [US2] #1 fix rider (Phase 4 re-walk, John 2026-07-15): confirm `/connect/spa-callback` + the web fetch flow preserve full PKCE semantics — (1) state validated (client compares returned state to sent state), (2) code verifier exchanged (OpenIddict `RequireProofKeyForCodeExchange` enforces this; cite the existing acceptance token-exchange coverage), (3) callback response non-cacheable (`Cache-Control: no-store` on spa-callback so the code-bearing URL response isn't cached), (4) the code read off `Response.url` can't be observed by other browsing contexts (fetch response URL is readable only in the initiating JS context; the page is never navigated to so no history entry; code is single-use + PKCE-bound + short-lived). One test or cited assertion each.
  - Done (2026-07-15): (1) client-side `state` validation added to `web_authorize.dart` (compares returned state to sent state, throws on mismatch); (2) code-verifier exchange cited — OpenIddict `RequireProofKeyForCodeExchange` + the existing `AcquireAccessTokenAsync` acceptance flow; (3) `Cache-Control: no-store` added to `/connect/spa-callback` with a new `SpaCallbackTests` acceptance assertion; (4) non-observability documented in the endpoint comment (fetch `Response.url` is same-JS-context-only, never navigated to → no history, code single-use + PKCE-bound). Verified: no-store present on the live endpoint; browser smoke test green.
- [X] T175 [US2] F1 — MFA enrollment broken in real browser (Phase 4 re-walk, John 2026-07-15; BLOCKER, green-but-dead #2). "Set up two-factor authentication" fails with the raw minimal-API error "Implicit body inferred for parameter 'request' but no body was provided" — `MfaEndpoints.EnrollOrConfirmAsync` declares a non-nullable `EnrollMfaRequest request` body, but the dual-mode start-enrollment call sends no body (the Dart client sends nothing for an empty-request POST; the C# acceptance test sends `{}`, which is why suites pass). **Fix on the correct side**: the OpenAPI already declares the requestBody optional (no `required: true`), so the C# is the side that contradicts the contract — make the body genuinely optional (`[FromBody(EmptyBodyBehavior.Allow)] EnrollMfaRequest?`), make `required: false` explicit in the OpenAPI, regenerate the client. Guard with a browser-tier test (MFA enroll shows the secret in a real browser).
  - Done (2026-07-15): confirmed the OpenAPI already declared the body optional (no `required: true`) — the non-nullable C# param was the contract violation. Fixed `EnrollOrConfirmAsync` to `[FromBody(EmptyBodyBehavior.Allow)] EnrollMfaRequest?` (+`request?.Code`), made `required: false` explicit in identity.yaml, regenerated the client (no drift — was already optional). Verified: POST /me/mfa with no body now reaches auth (401) instead of the body-inference 500; new browser test `web_auth_smoke_test.dart` (T175 case) shows the TOTP secret in real headless Chrome — this file is now the auth smoke set (T179). Backend Acceptance 42/42 (+SpaCallbackTests), MFA acceptance scenarios still green, frontend 58/58.
- [X] T176 [US2] F2 — sign-in state-machine defect (Phase 4 re-walk, John 2026-07-15; HIGH, trust-damaging). Reproduced: a failed sign-in shows "Invalid email or password"; returning to the sign-in page shows stale form data; submitting fails and the submit button becomes permanently disabled even after correcting input — yet navigating home reveals the user is actually signed in. So a sign-in succeeded without the UI transitioning, the submitting/disabled flag never resets on error, and form state isn't cleared. Audit the auth flow's state management end-to-end: success must navigate unconditionally, failure must re-enable submission, stale credentials must not persist across entries. Acceptance: a fail-then-succeed sequence in the headless-Chrome tier.
  - Done (2026-07-15): root cause was the navigation ordering — `sign_in_screen`/`register_screen` called `completePendingIntent(ref)` BEFORE `context.pop()`, so with a guest-gate pending intent, `resume()` pushed the target (e.g. /account) and the immediate `pop()` removed it, stranding the user back on sign-in (signed in but stuck — exactly John's report). Fixed: `completePendingIntent` now returns whether it resumed; the screens only self-navigate (pop/go) when NO intent was resumed, so a resumed intent's own navigation stands. Verified in the browser tier: guest-gated sign-in fail-then-succeed now (a) shows the error and re-enables submit on the failed attempt, and (b) on the correct attempt lands on `/account` (the intent target), never stranded on sign-in. The backend was never at fault — a wrong-then-correct login sequence returns 401 then 200 (confirmed by curl); the earlier confusion was a Flutter-web test-input quirk (a second enterText on a focused field doesn't replace), worked around by setting the controller directly in the test.
  - Follow-through (2026-07-15, John's checkpoint requirement): `completePendingIntent` has exactly two production consumers — `sign_in_screen` (covered above) and `register_screen` (audited via `requireAccount` call-site search: the only gated action in the app is the account-nav-button intent in `discover_screen.dart`, so this is the complete consumer set, not a sample). The register path was untested and is the other consumer the F2 fix could have silently broken. Added a browser-tier case: guest taps the gated account icon → prompt → completes the SAME captured intent via Register instead of Sign In → asserts registration signs in AND lands on `/account` (`accountMenuScreen`), never stranded on the register screen. Verified green in headless Chrome alongside the other three cases (4/4 pass).
- [X] T177 [US2] F3 — session not persisted across page reload (Phase 4 re-walk, John 2026-07-15; HIGH; ruling included). Refreshing the browser logs the user out (tokens are memory-only) — and with the 10-min access-token lifetime (T166 context) this means re-login every 10 minutes, so it's non-optional. **Ruling**: implement refresh-token rotation (OpenIddict one-time-use refresh tokens — enable if not already) with platform-appropriate persistent storage: secure storage on native; on web, persisted storage with the XSS exposure explicitly documented + a **CSP hardening task filed as a T139 launch-gate rider** (strict CSP is the compensating control). On app start: silent refresh → restored session; refresh failure → clean signed-out state, never an error. BFF/cookie-session alternative noted and **rejected for V1** as disproportionate — record both the decision and the rejected alternative in an **ADR**. **Addendum (from re-walk (e))**: reload currently forces re-login AND creates a new Active-sessions entry, so orphaned "active" sessions accumulate. Fold into acceptance: (a) **reload resumes, never duplicates** — reload silently restores the SAME session (assert session ID stable across reload in the headless-Chrome tier; sessions count unchanged, last-active updated; a new entry on reload is a failure); (b) **orphan hygiene** — define + implement session-expiry semantics (a session whose refresh token is expired/consumed-without-rotation must not render "active"); explicitly configure the refresh-token lifetime (state the chosen value, don't silently default); sessions list filters or ages-out dead sessions; clean up the orphans this bug already created in dev data; (c) **revocation linkage sanity** — since T166's denylist keys off sessions, confirm the restored-session identity is the same one `TryRevokeAsync` targets: revoke session from browser B while browser A is closed → A's next launch lands signed OUT, not silently resumed (test this). **T167 disposition rider (John 2026-07-15)**: while rebuilding refresh/rotation/session identity here, re-examine T167's flake (the `LastSeenAt` tie-break/session-index bug, resolved 2026-07-14) in light of the new design — either confirm the original root cause is unaffected by refresh-token rotation, or show the new design structurally closes whatever residual risk remains. T177's done-note **must include an explicit T167 disposition line**: resolved / still-watch-listed / escalated.
  - **ADR approved (2026-07-16)**: `docs/adr/0005-session-persistence-and-refresh-rotation.md` — core decision is silent `refresh_token` restore against the *existing* authorization on app start (never a fresh `authorization_code` grant), which derives stable session identity and revocation linkage from one mechanism. John's three riders folded in before implementation: (1) refresh-token lifetime is **14-day sliding + 90-day absolute cap**, both explicit (OpenIddict's rolling+sliding are confirmed on-by-default via the installed package's own XML docs, but the lifetime itself was never set; the absolute cap needs a custom check since OpenIddict has no built-in sliding+cap combination). (2) **Reuse detection is load-bearing security, not an implementation detail** — a stolen persisted refresh token gets one use before the legitimate client's next refresh trips it; new explicit acceptance test (redeem same token twice → second fails, authorization revoked). (3) **Multi-tab coordination is required, not optional** — one-time-use rotation + two open tabs racing to refresh is a self-inflicted "everyone's logged out" bug indistinguishable from T167's ghost; web needs a single-refresher election (Web Locks API, BroadcastChannel fallback), with its own acceptance test (two tabs across an expiry boundary, both stay signed in, zero revocations). T167 disposition recorded as **unaffected-but-not-yet-closed**: reasoning stands, but T177 rebuilds the exact path the flake lived on, so closure requires ≥10 repeated clean runs of the full revocation-adjacent acceptance set post-implementation, cited in T177's final report. Implementation tasks filed (#95-#102 in the task-tracking system); proceeding.
  - **Backend session-identity core DONE (2026-07-16)**: explicit `SetRefreshTokenLifetime(14 days)` in `IdentityModule.cs` (rolling+sliding already default-on, confirmed via the package's own XML docs — this makes the lifetime itself, previously silently inherited, a stated decision). 90-day absolute cap enforced in `TokenEndpoints.HandleTokenAsync`'s refresh_token branch against `SessionDevice.CreatedAt`, revoking + `Results.Forbid`-ing over-cap sessions the same way an explicit revoke does. Two new acceptance scenarios (18, 19) + a `TestTimeProvider` wired ONLY onto `OpenIddictServerOptions` (not globally in DI — see below) so scenario clock-advances actually move OpenIddict's own token-expiry/reuse-leeway timing, not just app-level `IClock` consumers. **Three real, empirically-confirmed findings while building this, each fixed, none guessed**: (1) OpenIddict's `RefreshTokenReuseLeeway` (30s default, confirmed via XML docs) legitimately tolerates a redeemed token being retried within that window — the reuse test must advance PAST it or a real reuse-detection regression would be indistinguishable from the leeway correctly doing its job (exactly the multi-tab-race shape ADR-0005 designs around). (2) OpenIddict's reuse detection revokes the individual redeemed **tokens**, not the **authorization** record (which stays `valid` — it's the overarching grant, not a token-lifecycle field) — confirmed via a temporary diagnostic (T167-precedent style); `SessionsEndpoints.ListAsync`'s orphan-hygiene check was rewritten to query live token status (`IOpenIddictTokenManager`, filtered to `TokenTypeIdentifiers.RefreshToken` — NOT `TokenTypeHints.RefreshToken`, a different RFC7009-vs-RFC8693 string that silently matched zero tokens and filtered every session out, also caught via diagnostic, also fixed) instead of authorization status. (3) globally registering the custom `TimeProvider` in DI also fed ASP.NET Core's cookie-auth handler, silently expiring the pre-existing T052 deactivation-warning scenario's cookie session on its 336-day clock jump — fixed by scoping the TimeProvider directly onto `OpenIddictServerOptions` only (a closure, no DI mediation), leaving cookie-auth on the real wall clock. **Verified**: full Acceptance suite 44/44, full local gate green (all 4 backend suites + frontend + drift + dependency-scan).
  - **Backend riders closed (2026-07-16)**: (1) scenario 18's "the session is no longer active" step now also asserts `Contains(freshSessionId)` — the prior `DoesNotContain`-only check would have trivially passed on an accidentally-empty sessions list, the exact shape of finding (2) above; standing rule recorded at `SessionsEndpoints.ListAsync`'s filter site: any filter against a provider-internal string constant needs a positive-match test, not only a negative one. (2)/(3) ADR-0005 updated with the leeway-vs-design test requirement and the cap/reuse clean-sign-out scope — see the ADR itself for full wording.
  - **Client TokenStore + restore flow DONE (2026-07-16)**: `core/auth/token_store.dart` (native = `flutter_secure_storage`, web = `shared_preferences`, chosen at runtime via `kIsWeb`, no conditional-import split needed since both packages compile on every target); `core/auth/session_restore.dart`'s `sessionRestoreProvider` reads the persisted refresh token on every app build and attempts a silent `refresh_token` grant via a new shared `silentlyRefreshTokens` function (extracted from `TokenRefreshInterceptor`, now used by both); `refreshTokenPersistenceProvider` reactively persists/clears `TokenStore` on every `refreshTokenProvider` change regardless of call site. **Design correction made empirically, not guessed**: a blocking splash gating the first frame on `sessionRestoreProvider` was tried and reverted — `pumpAndSettle` (used throughout this codebase's tests) settles on frame-scheduling, not arbitrary pending Futures, so a static splash with nothing animating gets judged "settled" instantly regardless of whether restore actually finished, breaking every existing test that pumps `SpecPourApp`; a spinner instead hung `pumpAndSettle` forever (animations never settle) — confirmed both failure modes by direct reproduction before landing on the current non-blocking design: `SpecPourApp` kicks off `sessionRestoreProvider` in the background and always renders the router immediately, exactly like any other async state the app already reacts to. **Second real finding**: `flutter_secure_storage`'s Linux backend needs a running D-Bus/libsecret service this sandbox doesn't have, hanging the platform channel indefinitely in `flutter test`'s VM target — fixed with a global `test/flutter_test_config.dart` mock (Flutter's own auto-applied test-directory convention, zero changes needed to any of the ~7 affected test files) rather than a real `Timer`-based timeout, which itself tripped a different Flutter test invariant ("Timer still pending after widget tree disposed") on any test finishing before the timeout fired. **Third real finding, caught by the browser tier itself**: real Chrome's localStorage genuinely persists across `testWidgets` cases sharing one page within a `flutter drive` run — a later case's fresh `pumpWidget` silently inherited an earlier case's persisted session via the new auto-restore, landing already-signed-in instead of guest; fixed with a `setUp` clearing `SharedPreferences` before each case in `web_auth_smoke_test.dart`. **Verified**: `flutter analyze` clean, `flutter test` 58/58, browser-tier smoke set 4/4 against a freshly rebuilt+migrated docker stack, full local gate green.
  - **Cross-tab election + mechanism test DONE (2026-07-17)** — #100 complete. Web single-refresher election (`refresh_coordinator.dart` + `cross_tab_channel_web.dart` Web Locks + synchronous localStorage handoff + BroadcastChannel; `cross_tab_channel_stub.dart` native no-op; both refresh callers route through `coordinatedRefresh`). Implementation first committed explicitly-not-done (`aaa0859`) to protect the no-regression-verified work through the flaky-probe debugging session, per John's rider.
    - **Harness (down John's ladder)**: two real *tabs* aren't drivable in the single-context `flutter drive` harness — a `window.open` probe **wedged the run** (killed after 11 min, evidence in ADR-0005). Resolved with a same-origin **`<iframe>` second agent** (John's variant (b)): one bundle, `main()` branches on `web.window.frameElement` — top frame = the driven orchestrator, iframe = a minimal UI-less agent running the unmodified production `coordinatedRefresh`. An iframe is a child of the one driver-controlled document (no second window handle), so it doesn't wedge the driver — confirmed by a feasibility gate before building.
    - **Assertion is the MECHANISM (Rider 1)**: a Development-gated server-side refresh-**ATTEMPT** counter (`RefreshAttemptCounter` + `GET /dev/refresh-attempts`, both env-gated, zero prod footprint) — attempts not successes, because a broken election whose two requests both land inside OpenIddict's 30s reuse leeway would BOTH succeed and false-green an outcome-only test. Asserts exactly **1** attempt.
    - **Deterministic contention (Rider 2)**: the orchestrator pre-acquires the production lock and HOLDS it, sends "go" so both contexts' `coordinatedRefresh` requests queue behind it, polls `navigator.locks.query()` until 2 are pending (barrier genuinely engaged), then releases — guaranteeing a real race, not a fast-localhost sequential non-race.
    - **A REAL election bug the mechanism test caught** (its whole point): the first design had the adopter read the rotated token from `shared_preferences` — but that write is async/fire-and-forget AND cached per-instance, so a *different* tab never saw the winner's rotation → both refreshed (2 attempts, under genuine contention). Fixed with a **synchronous `window.localStorage` handoff keyed by the replaced token (`from`)**: written before the lock releases, read back synchronously+live by the queued tab — deterministic and stale-safe. The BroadcastChannel is now a best-effort passive-tab optimisation only; correctness no longer depends on it. (This does briefly persist the access token in the handoff — accepted, same threat model as the refresh token; ADR-0005 updated.)
    - **Verified**: mechanism test passes **4/4 repeated runs** (deterministic, non-flaky); `flutter analyze` clean; `flutter test` 58/58; existing browser tier 4/4 (single-tab path unregressed by the handoff redesign); backend Acceptance 44/44 (counter/endpoint no-regression). Wired into CI (`frontend-web-browser-tests` now runs both the smoke set and `web_multitab_test.dart`). ADR-0005's "Cross-tab coordination — as built" section updated with the handoff design + the bug analysis. **NOTE for #102**: the mechanism test's repeated-run cleanliness is part of the revocation-adjacent evidence set the T167 disposition needs — fold it into the ≥10-iteration run.
  - **#100 item-0 riders DONE (2026-07-17)** — two small riders John queued right after #100 landed, before #101.
    - **(a) Handoff hygiene**: `readHandoffFor` deliberately never deleted the entry itself (a naive delete-on-first-adopt was traced through a concrete 3-tab scenario and rejected before implementation: tab A wins and writes, tab B adopts-and-deletes, tab C then finds nothing and retries its own already-redeemed token — a real reuse-detection trip for a session that was actually fine). Added `maybeClearHandoffAfterDrain` (called by both the winner after writing and the adopter after reading, right before releasing the lock) which clears the handoff only when `navigator.locks.query()` shows nobody else still queued — i.e. only whichever context is provably the *last* reader of a given contention burst clears it, so 3+-tab races stay correct. Backstopped by `sweepOrphanedHandoff` (30s TTL, judged from the handoff's own timestamp), wired into `crossTabAuthSyncProvider` so it runs on every app boot — covers a tab that closed before its own drain-triggered cleanup could fire. Judged as additive cleanup, not an election-logic change, so proceeded without escalating per the item's own conditional. `web_multitab_test.dart` gained two assertions: the existing race scenario now also asserts no handoff entry remains once both contexts have resolved, and a new case seeds an artificially-aged entry then boots `SpecPourApp` and asserts it's swept. **Verified**: `flutter analyze` clean, `flutter test` 58/58 (no regression), multitab tier **4/4 repeated runs** including the new hygiene assertions, existing single-tab smoke tier unregressed.
    - **(b) Dev-surface gating**: `DevOnlyRefreshAttemptsEndpointTests.cs` — asserts `/dev/refresh-attempts` 404s under a `Production`-configured host (route never mapped, not mapped-but-401) and that a real register→authorize→refresh round trip completes normally under `Production` (the counter-increment skip doesn't disrupt anything). **A real, empirically-confirmed regression found and fixed, not guessed**: running the full backend Acceptance suite after adding this file surfaced `RateLimitingTests.AnonymousTrafficIsRateLimitedWith429` failing with an OpenIddict `ConcurrencyException`/`DbUpdateConcurrencyException` inside `OpenIddictClientSeedingHostedService.StartAsync` — root-caused (reproduced 5/5 with the two classes run together, 0/5 with either alone) to both test classes independently booting their own `SpecPourWebApplicationFactory` (needed for environment-shaped concerns `AcceptanceHooks.Factory`'s shared Development host doesn't exercise), each re-running the seeding service's sync-on-every-boot `UpdateAsync` against the *same* seeded OpenIddict application row with no isolation between them. `RateLimitingTests.cs`'s own doc comment already claimed a `[Collection]` attribute pinned it against exactly this class of collision — but the attribute was never actually present on the class, only described in the comment; it went unnoticed because nothing else in the suite shared the own-separate-factory pattern until this file did. Fixed by adding `[Collection("ReqnrollNonParallelizableFeatures")]` (Reqnroll's own generated collection name) to both classes, closing both the new regression and the previously-inert gap in the old one. **Verified**: combo of the two classes run together **5/5 clean post-fix** (was 5/5 failing pre-fix); full backend Acceptance suite **46/46, 2/2 repeated runs**.
  - **#101 (a)+(b) DONE, (c) ESCALATED (2026-07-17)** — `web_session_persistence_test.dart`, new headless-Chrome tier file.
    - **(a) reload resumes the same session**: fresh sign-in → 1 session → simulated reload → still 1 session, same `id`, `authTokenProvider` restored, `lastSeenAt` strictly bumped by the restore-triggered refresh.
    - **(b) revoked-while-closed**: sign in → revoke the session (self-service call, dogfooding `SessionsScreen`'s own `revokeSession` — server-side revocation is stateless and doesn't care which context issued the call, so this is faithful to what the scenario actually tests: the client's reaction on its NEXT launch, not literal two-context mechanics, unlike #100's genuine-race mechanism test) → next launch lands `authTokenProvider == null`, no exception (`tester.takeException()`), and the normal discover shell renders (`discoverBrowseList` found) — never a stuck/error screen.
    - **A real, empirically-confirmed bug in the TEST HARNESS, caught and fixed, not guessed**: the first version of `_bootApp` simulated "reload" by calling `pumpWidget(ProviderScope(...))` a second time within ONE `testWidgets` case. Root-caused via reproduction (added temporary diagnostics, then a direct `curl`-based repro against the live API proving the BACKEND correctly rejects a revoked refresh with `400 invalid_grant` — ruling out a completed-task defect before suspecting the test): Flutter's element diffing treats a second `pumpWidget` call with the SAME root widget type as an UPDATE, not a remount, so `ProviderScope`'s `State` (and its `ProviderContainer`, including in-memory `authTokenProvider`) survives untouched — "reload" was silently a no-op rebuild of the SAME already-signed-in instance. Confirmed by two symptoms: `lastSeenAt` was byte-identical across the "reload," and a revoked session's access token was still readable after "relaunch" (no second network round trip ever happened). This is a DIFFERENT mechanism from T099's own precedent (localStorage persisting across SEPARATE `testWidgets` CASES, which DOES get a real teardown between cases) — that finding does not extend to multiple `pumpWidget` calls inside ONE case. Fixed: `_bootApp` now pumps an unrelated widget (`SizedBox.shrink()`) first to force a genuine unmount before remounting the app, guaranteeing a fresh `ProviderContainer` each call. Re-verified: both cases pass, **4/4 repeated runs**; existing single-tab smoke tier and the #100 multitab tier both re-run and unregressed; `flutter test` 58/58; `flutter analyze` clean.
    - **A second real, CI-only finding, also caught and fixed**: pushed and passed 4/4 locally, but CI's `frontend-web-browser-tests` (a required check) failed on (b)'s screen-shell assertion. Root-caused from the CI log: `DiscoverScreen`'s `_BrowseList.when()` only renders the `discoverBrowseList` key on a SUCCESSFUL, NON-EMPTY catalog fetch — its `error` and empty-`data` branches render `discoverBrowseEmpty` text instead. CI's docker stack has no catalog-seeding step (unlike the long-running local stack this was developed against, which had accumulated real recipe data), so the assertion was environment-dependent by construction, not a timing issue. Fixed by switching to `discoverSearchField` — part of `DiscoverScreen`'s fixed chrome, built unconditionally outside the async-gated `_BrowseList`, so it's a correct, environment-independent proof of "landed on the normal shell" regardless of catalog contents. Re-verified locally **3/3**; re-pushed for CI (see commit history for the CI-green confirmation).
    - **(c) 90-day-cap-expiry — RULED and DONE (2026-07-17).** John's ruling on the escalation above: modified Option 3 (test-script-side DB backdate, zero application code, zero new API surface) for the cap only; **Option 2 (a Development-gated clock-control endpoint) is REJECTED PERMANENTLY** — "a live clock-control surface is an attack-surface-shaped object whose test value we just showed to be near zero," recorded here so it is not re-proposed. Reuse-detection-revocation's live trigger is **accepted as covered without a dedicated live test**: backend Acceptance scenario 18 already proves the server-side mechanics (redeem-twice → second fails, authorization revoked), and (b) already proves the client's reaction to a `Forbid`'d refresh end-to-end through a real browser — reuse detection produces the exact same `Forbid` shape the cap and explicit-revoke branches do (`TokenEndpoints.HandleTokenAsync`'s cap branch and `SessionsEndpoints.RevokeAsync` both call `TryRevokeAsync` + the same rejection path), so there is no separate client code path reuse detection could break that (b) doesn't already exercise; leeway-reconfiguration infrastructure to trigger reuse live was deliberately NOT built.
    - **Delivered**: `web_cap_expiry_test.dart` + `scripts/run-cap-expiry-test.sh`. The Dart test registers a uniquely-tagged (`capexpiry-`) account, sleeps 20s, cold-restarts, and asserts the client lands cleanly signed out — same shape as (b). The wrapping script polls Postgres directly for that run's freshly-created `SessionDevices` row (filtered to strictly newer than a captured baseline, so a leftover row from an earlier interrupted run can never be mistaken for the current one) and backdates its `CreatedAt` to 91 days ago via a direct `psql` `UPDATE` while the Dart side sleeps.
    - **A real, empirically-confirmed harness dead-end, caught and fixed, not guessed**: the first version had the Dart test `print()` its session id and the script grep `flutter drive`'s captured stdout for the marker. This NEVER worked, regardless of how long the script waited — confirmed by direct inspection of full captured logs showing the marker line simply never appears anywhere in the output. Root cause: `print()` calls from code running inside the browser under `flutter drive -d web-server --headless` do not surface in the driver process's own stdout at all on this target — not a timing issue, a dead channel. Switched to having the script poll Postgres directly for the tagged row instead, which needs no signal from the Dart side. Separately, also needed two timing widenings once the DB mechanism itself was confirmed correct via a direct `curl`+`psql` repro (backdate → `400 invalid_grant` on the next refresh, confirmed BEFORE debugging the harness further, ruling out a backend defect): the script's polling window (60 → 150 one-second checks) and the Dart side's pre-restart sleep (8s → 20s), since the initial page-load-to-first-connection alone was observed taking 25s+.
    - **Verified**: SQL mechanism confirmed correct via direct repro; full script **3/3 repeated runs**; existing multitab and session-persistence browser tiers re-run and unregressed; `flutter test` 58/58; `flutter analyze` clean. Wired into CI (`frontend-web-browser-tests` gained a fourth step).
    - **Harness guard filed (bug-1 pattern)**: the pumpWidget-twice-is-a-silent-no-op trap from earlier in #101 will bite again on any future reload-style test — factored `_bootApp` into a shared `integration_test/support/cold_restart.dart` helper (`coldRestart`), with its own doc comment explaining the trap, and refactored `web_session_persistence_test.dart` + this file onto it. Also factored the duplicated registration UI flow into `integration_test/support/register_fresh_account.dart` (`registerFreshAccount`) while touching this, so the two files' account-creation steps can't silently drift apart.
    - **Standing rule filed (bug-2 pattern)**: post-auth landing assertions must target UNCONDITIONAL screen chrome, never data-dependent widgets — recorded as a comment at both files' `discoverSearchField` assertion sites (the actual convention-carrying location, per this codebase's existing `integration_test/support/` pattern — there is no separate conventions document).
    - **Flagged, not built**: CI's docker stack has zero catalog-seeding step. That was adequate for every auth-focused browser-tier test so far (this one included — `discoverSearchField` sidesteps it entirely), but it will become inadequate the moment a Phase 5 content-phase test needs to assert against actual catalog data. Filed as **T181** below so it's a planned item, not a surprise.
  - **#102 — T167 disposition CONFIRMED CLOSED post-T177 (2026-07-17)**: see T167's own entry above for the full evidence (scenarios 15/18/19 10/10 clean, folded with #100's mechanism test's 8 clean runs). Cross-referenced here per this close-out's own enumeration requirement, not duplicated in full.
  - **T177 CLOSE-OUT (2026-07-17) — every rider disposition, in one place**:
    - **Multi-tab election (#100)**: DONE. Web Locks + synchronous localStorage handoff + BroadcastChannel-as-optimisation. A real election bug (adopt path reading a `shared_preferences` write that's async/per-instance, so a different tab never saw it) found and fixed via the mechanism test itself. Verified 4/4 repeated runs.
    - **Handoff hygiene + dev-surface gating (#100 item 0)**: DONE. Lock-queue-gated `maybeClearHandoffAfterDrain` (correctness-checked against a 3-tab scenario before implementing, not guessed) + 30s-TTL `sweepOrphanedHandoff` backstop. `/dev/refresh-attempts` confirmed inert and 404'd under Production. A real pre-existing test-isolation gap (`RateLimitingTests`'s doc-commented-but-never-actually-applied `[Collection]` attribute) found and fixed alongside.
    - **Reload resumes the same session (#101a)**: DONE. Session ID stable, count unchanged, `LastSeenAt` bumped. 4/4 repeated runs.
    - **Revocation linkage / revoked-while-closed (#101b)**: DONE. A revoked session's next launch lands cleanly signed out, no error screen. 4/4 repeated runs. (Two real bugs found and fixed along the way — see #101's own entries: the pumpWidget-twice harness trap, and the CI-only `discoverBrowseList`-depends-on-seeded-catalog assertion fragility.)
    - **90-day absolute cap, live (#101c)**: DONE via test-script-side Postgres backdate (John's ruling) — zero application code, zero new API surface. 3/3 repeated runs.
    - **Reuse-detection, live**: DELIBERATELY NOT built as a dedicated browser test (John's ruling) — accepted as covered by backend Acceptance scenario 18 (server-side mechanics) plus #101b's browser-tier proof of the client's reaction to the identical rejection shape. Building live leeway-reconfiguration infrastructure was judged to only re-prove an already-proven client seam.
    - **Option 2 (Development-gated clock-control endpoint)**: REJECTED PERMANENTLY (John's ruling) — recorded in ADR-0005 so it is not re-proposed.
    - **T167 line**: CONFIRMED CLOSED post-T177 (see above and T167's own entry) — not merely carried over from its pre-T177 2026-07-14 closure.
    - **Harness guards + standing rules filed**: shared `coldRestart`/`registerFreshAccount` helpers (`integration_test/support/`) close the pumpWidget-twice trap for every future reload-style test; the unconditional-screen-chrome-only rule for post-auth landing assertions is documented at its usage sites (no separate conventions doc exists in this codebase). T181 filed (not built) for CI's zero-catalog-seed stack, ahead of Phase 5 needing it.
    - **Walkthrough**: `docs/phase4-walkthrough-checklist.md`'s F3 disposition updated from STILL OPEN to FIXED — section (f)'s persistence-toggle check (previously "Unable to test - refresh logs out user") is now mechanically verifiable, confirmed via #101a's automated browser test, not self-certified as PASS — flagged for John's own re-walk of (e) and (f), per this project's standing human-visual-verification rule for walkthrough-style checks.
    - **Full local gate + CI**: every item above landed with a green full local gate and green required CI checks (commits `35ee70d` through `8a72024`, PR #2, branch `t177-session-persistence`) before the next item started.
- [X] T183 [US2] Frozen-tab refresh hardening (John's PR #2 merge review of `refresh_coordinator.dart`, 2026-07-17). A tab Chrome froze in the background misses the BroadcastChannel update; if the contention burst drained in the meantime, the handoff is cleared; on resume the tab hits a 401 and elects itself with its stale in-memory refresh token — presenting an already-redeemed token far outside the 30s leeway, tripping reuse detection, and revoking the entire session (every tab logged out — the exact benign-race logout the election exists to prevent). Fix: inside `withRefreshLock`, after the primary handoff check misses, live-read the persisted refresh token directly from `window.localStorage` (never through `SharedPreferences`) and, if it differs from the starting token, use it for both the handoff lookup and the refresh itself.
  - **Done (2026-07-17)**. Real key name confirmed empirically, not assumed: a temporary diagnostic dump of every `window.localStorage` key after a real sign-in (deleted after use) showed `shared_preferences_web` stores the token under `flutter.specpour.refreshToken` (the `flutter.` prefix and JSON-string-encoded value both confirmed against the package's own source, `shared_preferences_web-2.4.3/lib/shared_preferences_web.dart`), not just inferred from the well-known convention. `cross_tab_channel_web.dart` gained `liveReadPersistedRefreshToken()` (raw synchronous `window.localStorage.getItem` + `jsonDecode`); `token_store.dart`'s key constant made public (`refreshTokenKey`, was `_refreshTokenKey`) so both files share one source of truth instead of a second, driftable copy. `token_store.dart`'s `WebLocalTokenStore` gained the requested maintainer comment: its reads can be stale cross-tab (per-instance `SharedPreferences` cache), and cross-tab correctness lives entirely in the coordinator/handoff, not this class.
  - **A deliberate deviation from the literal spec, reasoned through and documented, not silently made**: the spec said live-read "before the handoff check," always. Implemented instead as a FALLBACK consulted only after the primary handoff lookup (keyed by the caller's own starting token) misses. Reasoning: `refreshTokenPersistenceProvider` persists reactively via `unawaited(...)` (`api_client_provider.dart`) — a winner's own critical section can complete before that persist actually lands in localStorage. An unconditional live-read at the very top could therefore occasionally observe a peer's still-in-flight rotation and mis-key the FIRST handoff lookup with an already-rotated token that doesn't match the handoff's `from`, turning a genuine adopt into a spurious extra refresh — undermining the existing multitab mechanism test's "exactly one attempt" invariant on pure timing, which the task explicitly required to "stay green untouched." Ordering the live-read as a fallback makes the new code path provably unreachable in that test's own scenario (the primary lookup there always already resolves the race correctly), while still fully satisfying the requirement's actual intent (using the live value for both the handoff lookup and the refresh, in the frozen-tab case it exists for) — additive, not a change to the election/handoff/broadcast mechanics themselves, so not escalated.
  - **(a) unit test**: `selectEffectiveRefreshToken` extracted as its own pure function (no platform I/O) specifically so this decision is testable without a real web platform — `test/core/auth/refresh_coordinator_test.dart` (3 cases: prefers a differing live token, keeps the starting token when they match, keeps it when there's no live token).
  - **(b) browser-tier test**: new `web_frozen_tab_test.dart` — deliberately SEQUENTIAL (not the multitab test's simultaneous-contention barrier): the orchestrator completes its own lone refresh (T0→T1, drains immediately since uncontended) BEFORE the "frozen" iframe agent — which captured T0 at boot, before the orchestrator's rotation — presents that stale T0. Asserts: the agent's refresh succeeds (session survives); `GET /me/sessions` still shows exactly 1 live session (zero revocation — a trip would have filtered it out); `/dev/refresh-attempts` reads exactly 2 (the orchestrator's rotation + the agent's ONE corrected attempt, never a wasted attempt on the stale token first, by construction). **Verified 4/4 repeated runs.**
  - **Existing multitab mechanism test confirmed unaffected, empirically, not just reasoned about**: re-run 5/5 clean after this change landed (unmodified from its own prior verification).
  - **Verified**: `flutter analyze` clean, `flutter test` 74/74 (+3, no regressions), all four existing browser-tier files (smoke set, multitab, session-persistence, cap-expiry, export-download) re-run and unregressed, new frozen-tab test 4/4. Wired into CI (`frontend-web-browser-tests` gained a sixth step).
- [X] T184 Fail-open sweep of all CI/gate checks (John's directive batch, 2026-07-17 — the resurrected post-T168 rider, this time filed; third instance of the class was the `| tee` exit-code masking found during T173's local gate run). Audit every check in `.github/workflows/ci.yml` and `scripts/pre-push-checks.sh` for the passes-on-inconclusive shape, with shell pipeline semantics explicitly in scope. Apply `set -o pipefail` alongside `set -e` in all gate scripts, then force one piped step to fail and confirm the gate actually fails (empirical, not reasoned). Deliverable: a verdict table — every check marked fail-closed / hardened-in-this-task / N-A — covering at minimum: dependency scan (already hardened, confirm), client-drift (fails if regeneration itself errors?), coverage gate, browser job's docker health-wait (stack never up = job fails, not zero-tests-pass?), Trivy step (behavior on scan-infrastructure error), format/analyze steps (post-pipefail), and any check added since T168. Doubles as Principle 18's first compliance audit (see T186).
  - **Done (2026-07-17)**. Headline finding: every committed gate script already had `set -euo pipefail` — the live gap was **ci.yml itself**: GitHub Actions' DEFAULT Linux shell for `run:` steps is `bash -e {0}` **without** pipefail (pipefail only comes with an explicit `shell: bash`, which GH invokes as `bash --noprofile --norc -eo pipefail {0}`). Confirmed empirically, not from docs alone: the same `false | tee` script exits 0 under the default invocation and 1 under the explicit one. So ci.yml's two piped steps (backend-tests, dependency-scan) were surviving on their grep guards alone. **Hardened**: workflow-level `defaults.run.shell: bash` (one change covers every current and future run step), with an explanatory comment. Also root-caused the T173-session masking instance precisely: it was the *invoking* shell's `script | tee log` wrapper (an ad-hoc pattern, not committed code) — the gate script itself was never fail-open; noted here because the class extends to how gates are *called*, not just how they're written.
  - **Forced-failure confirmations (empirical)**: (1) sabotaged the real local gate's piped `dotnet test | tee` step with `false | tee` → `pre-push-checks.sh` exited 1 immediately (pipefail doing its job), then restored from git; (2) ran ci.yml's exact backend-tests grep guard against three output shapes — failing suite ("Failed! …"), crashed-before-running (MSBuild error, no summary line), genuine pass — guard blocks the first two, passes only the third.
  - **Verdict table (Principle XVI's first compliance audit)**:
    | Check | Where | Verdict |
    |---|---|---|
    | backend-build (restore/build) | ci.yml | fail-closed (plain commands, nonzero propagates; analyzers enforced via Directory.Build.props) |
    | backend-tests (4 suites) | ci.yml | fail-closed + **hardened**: grep guard requires `Passed!…Total: ≥1` (verified against 3 shapes above); pipeline exit itself now also load-bearing via workflow pipefail |
    | frontend-analyze / dart format | ci.yml | fail-closed (plain commands; `--set-exit-if-changed` exits 1 on drift — this exact exit code is what the T173 outer-tee incident masked and pipefail now protects) |
    | frontend-unit-tests | ci.yml | fail-closed (plain `flutter test`) |
    | frontend-integration-tests (Android) | ci.yml | N/A by design — `continue-on-error: true`, not in required checks, expected-red until T180; its own steps still report true pass/fail |
    | browser job: docker health-wait | ci.yml | fail-closed (explicit `exit 1` after 60×2s; stack-never-up = job fails, never zero-tests-pass) |
    | browser job: 6 test steps (incl. everything added since T168: multitab, session-persistence, cap-expiry, export-download, frozen-tab) | ci.yml | fail-closed (all three wrapper scripts have `set -euo pipefail`; `flutter drive` exit propagates — cap-expiry's `wait "$DRIVE_PID"` and export-download's find/comm + python content validation each end in explicit `exit 1` error paths; targets are fixed files, so zero-tests = compile failure = nonzero) |
    | client-regen-drift-check | ci.yml + script | fail-closed (both `check-client-drift.sh` and `generate-client.sh` have `set -euo pipefail`; a regeneration *error* aborts the script nonzero before the diff — it cannot read as "no drift") |
    | dependency-scan (NuGet) | ci.yml | fail-closed (confirmed): completion-marker grep ("The following sources were used") fails the step on an incomplete advisory query; vulnerable-marker grep fails it on findings; pipeline exit now also unmasked |
    | dependency-scan (`dart pub outdated`) | ci.yml | N/A by design — explicitly labeled advisory/informational, `|| true` is the visible acceptance the principle requires |
    | container-scan (Trivy) | ci.yml | fail-closed **by documented tool behavior, not empirically forced** (trivy not installed locally): `--exit-code` only governs the code on *findings*; operational/scan-infrastructure errors return nonzero regardless, failing the step. Flagged honestly rather than claimed as verified |
    | coverage gate | — | N/A — **no coverage gate exists anywhere in this repo** (grep across workflows + scripts: zero hits). Not silently skipped: whether to add one is a product/process decision for John, not something this sweep invents |
    | pre-push-checks.sh (all 4 sections) | local gate | fail-closed (already had `set -euo pipefail` + the same test-count grep guard as CI; forced-failure-confirmed above) |
- [X] T185 SMTP adapter null/blank-password construction test (John's directive batch, 2026-07-17 — the other decayed post-T168 rider). Cite the existing test asserting `SmtpEmailChannelAdapter` construction throws on null/blank password, or add it.
  - **Done (2026-07-17)**: cited + extended. The existing test was `SmtpEmailChannelAdapterContractTests.SendAsync_rejects_a_configured_username_with_no_password` (added 2026-07-16 with the MailKit-CVE fix) — it covered **null** only, and asserts at `SendAsync`, not construction. The guard's placement at SendAsync is deliberate, not a gap: the adapter is DI-constructed with `IOptions<SmtpEmailOptions>` whose `.Value` is intentionally not read until send time, so send time is the earliest point the bad config is observable — a construction-time assertion would fight the options pattern. Extended the test to a `[Theory]` covering null, `""`, and whitespace (`ThrowIfNullOrWhiteSpace` rejects all three; each now pinned by its own case), renamed to `SendAsync_rejects_a_configured_username_with_a_null_or_blank_password`, doc comment updated with the placement rationale. **Verified**: filtered run 7/7 (3 theory cases + the 4-case shared adapter contract) against a real Testcontainers smtp4dev.
- [X] T186 Constitution reconciliation — Fail-Closed Verification principle (John's directive batch, 2026-07-17). Source constitution (`docs/constitution-statement.md`, reviewer-owned, updated 2026-07-17) added Principle 18 — Fail-Closed Verification. Reconcile the generated spec-kit constitution (`.specify/memory/constitution.md`): bump version, map the new principle into its numbering, and note in its changelog that it codifies the pattern found empirically three times (dependency scan silent-incomplete, token-type filter, `| tee` exit-code masking). T184's verdict table doubles as the principle's first compliance audit.
  - **Done (2026-07-17)**: source Principle 18 mapped in as **Principle XVI — Fail-Closed Verification**, prose converted to the generated document's MUST-normative register, content otherwise faithful to the source (which stays untouched — reviewer-owned canon). Version **1.5.0 → 1.6.0** (MINOR: new principle, per the document's own versioning policy). Sync Impact Report rewritten for this amendment, including the changelog note John specified: the principle codifies a pattern found empirically three times before being written down — (1) NuGet dependency scan silently passing on an incomplete advisory query (T168, hid real MailKit/MimeKit CVEs for weeks), (2) the orphan-hygiene token-type filter matching zero rows and reading as success (T177), (3) `| tee` exit-status masking (T173's gate run + latent in ci.yml's default-shell run steps, fixed by T184). Template propagation per the amendment procedure: `plan-template.md` gained Constitution Check gate XVI ("what does it do when it cannot tell?"); spec/tasks/checklist templates confirmed aligned (the principle governs pipeline construction, not feature specs). `CLAUDE.md`'s stale constitution version pointer (still said v1.4.0) corrected to v1.6.0 while touching this.
- [X] T178 [US2] F4 — data export must produce a portable artifact (Phase 4 re-walk, John 2026-07-15). "Export my data" currently renders on-screen only, unselectable. The lifecycle requirement is data portability: export must **download a machine-readable JSON file** containing the export payload; the on-screen view is a courtesy rendering (becomes selectable via T172). Extend the (h) acceptance test: export produces a downloadable artifact whose contents match the API payload.
  - **Done (2026-07-17, FR-003a)**: `account_data_screen.dart`'s existing export button now also triggers a real browser download (`specpour-data-export-{userId}.json`, the full `MeExport` payload via `standardSerializers.toJson`) — the on-screen DOB/email rendering is unchanged, now genuinely a courtesy view rather than the only surface. New `core/download/file_download_web.dart` + `file_download_stub.dart` (conditional-import pair, matching the established `web_authorize.dart` pattern): Blob + object-URL + synthetic-anchor-click, the standard client-side download mechanism. Gated on `kIsWeb` (a runtime check, not just the conditional import) — T178 is scoped to web for V1, native file-save/share-sheet UX is future work, not silently broken.
  - **A real regression found and fixed while building this, not guessed**: gating on the conditional import alone broke the pre-existing `account_management_widget_test.dart` case that exercises the export button — `flutter test`'s Dart-VM harness resolves the conditional import to the stub (no `dart.library.js_interop`), and the stub's deliberate `UnsupportedError` propagated up through the whole export flow, not just the download. Fixed by additionally gating on `kIsWeb` (false in both the VM-test harness and true native builds, true only in an actual compiled-for-web run), so non-web contexts skip the download call entirely instead of hitting the stub's throw.
  - **Browser-tier verification, a real download confirmed on disk, not just a function call**: `web_export_download_test.dart` + `scripts/run-export-download-test.sh`. The Dart side can't verify the download itself (`dart:io` isn't available in code compiled for the web target it drives), so — same host/browser split as T177 #101(c)'s cap-expiry test — the wrapping script checks the host's `~/Downloads` directory for the new `specpour-data-export-*.json` artifact after the browser test completes, and validates its JSON content (userId/email/sessions present) before cleaning it up. Confirmed empirically: headless Chrome under `flutter drive`'s default WebDriver session writes downloads to `~/Downloads` with no extra chromedriver capability configuration needed. **Verified**: 3/3 repeated runs, real file downloaded and content-validated each time; `flutter analyze` clean; `flutter test` 58/58 (regression fixed); existing browser tiers unregressed. Wired into CI (`frontend-web-browser-tests` gained a fifth step).
- [ ] T179 [US2] Grow the browser-context test tier into a deliberate auth smoke set (Phase 4 re-walk structural item, John 2026-07-15). Three green-but-dead instances (registration, MFA enroll, session persistence) share one shape: correct backend + passing suites + broken real-browser behavior. The headless-Chrome tier built for T169 (`scripts/run-web-integration-tests.sh`) is the answer — extend it to cover the auth-critical journeys: register, sign in (fail-then-succeed, T176), MFA enroll→confirm→disable (T175), reload-restores-session (T177), export download (T178) — and wire it into the CI task already filed (`.github/workflows/ci.yml` / T168's local gate). **This tier is now load-bearing: treat additions to it as part of any task touching auth or the web client.**
  - Progress note (2026-07-15): CI-wiring half done as part of T168 — new `frontend-web-browser-tests` job runs the current smoke set (register, sign-in fail-then-succeed, register-path intent, MFA enroll — 4 cases) on every push, verified green. Still open: growing the suite itself (MFA confirm/disable, reload-restores-session once T177 lands, export-download once T178 lands).
- [ ] T180 Android-emulator CI job needs real KVM/acceleration (discovered 2026-07-15, T168 verification). `frontend-integration-tests` (`reactivecircus/android-emulator-runner`, pre-existing, not part of T168's scope) hung 1h+ on emulator boot on `ubuntu-latest` before being cancelled by hand; a second run then failed cleanly at 16m39s once bounded by the `timeout-minutes: 20` T168 added as an interim safety net — consistent with `ubuntu-latest` lacking reliable KVM/hardware acceleration for the x86_64 emulator. Investigate: whether GitHub's hosted runners support nested virtualization for this use case at all (some hosted tiers do, some don't), an alternative emulator/acceleration configuration, or moving this job to a different runner. Until resolved, this job is expected-red and non-blocking by design (the 20-minute bound is the safety net, not the fix).
  - **Root-cause correction (2026-07-16, before acting)**: the "`ubuntu-latest` lacks KVM" framing (mine and a Copilot analysis John relayed) is very likely WRONG. GitHub's standard Linux hosted runners have had nested virtualization/KVM for a while; the pasted failure signature (device offline ~10 min, then broken-pipe, exit 224) is the classic **KVM-present-but-not-permissioned** case, not KVM-absent. The `reactivecircus/android-emulator-runner` project's own documented fix is a one-time udev step to grant the runner user access to `/dev/kvm`:
    ```yaml
    - name: Enable KVM
      run: |
        echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
        sudo udevadm control --reload-rules
        sudo udevadm trigger --name-match=kvm
    ```
    usually paired with AVD snapshot caching (`actions/cache` on `~/.android/avd`). **Do NOT take Copilot's "switch arch x86_64 → arm64-v8a" suggestion** — the runner is an x86_64 host, so an arm image runs under full QEMU CPU emulation (no hardware accel), which is slower, not faster. Planned fix when T180 is worked: add the KVM-enable step + a `ls -la /dev/kvm` / `kvm-ok` diagnostic to confirm the hypothesis, add AVD caching, then drop `continue-on-error` and add it back to the required-checks set once green. John's call (2026-07-16): keep T177 the priority, do this as a small separate follow-up PR.
- [ ] T181 Flagged, not yet built (T177 #101(c), 2026-07-17): CI's `frontend-web-browser-tests` docker stack has zero catalog-seeding step. Adequate for every auth-focused browser-tier test to date (all of them assert against screen chrome or session/auth state, never catalog contents), but it will become inadequate the moment a Phase 5 content-phase browser test needs to assert against actual recipe/catalog data — `DiscoverScreen`'s `_BrowseList` renders `discoverBrowseEmpty` text instead of real content against an empty catalog, exactly as caught (as a CI-only failure) during #101(b)'s work. Scope when worked: a minimal, deterministic seed fixture (a handful of known recipes) loaded into CI's stack before the browser-tier job runs, distinct from `Tools/Seeder`'s fuller dev-data generator — small and fast enough not to meaningfully extend the job's runtime.
- [ ] T140 SC sweep with explicit verification methods in backend/tests/Acceptance/SuccessCriteria/: automated (SC-003–SC-007, SC-009–SC-011, SC-013, SC-015–SC-017 via acceptance/contract suite tags; SC-002's search-latency half via T137); client-verified (SC-002's 3-interaction depth via a navigation-depth assertion in the Flutter integration suite; SC-014 via T125's integration test plus an on-device wakelock/voice checklist); semi-automated (SC-008 dataset-driven via the T147 harness; SC-012 via the T137 load rig); manual protocol (SC-001 timed UX walkthrough, documented checklist)
- [ ] T141 [P] ADR backlog: record R1–R18a decisions as ADRs, including the ai/search persistence decision (ai schema owned by the AI module; search owns no schema — an interpretation of Principle III) and the notifications ADR's Principle XI anti-abuse deferral rationale (V1 defers frequency caps/rate limits because its only senders are two platform alert types — prep expiry, account-deactivation warning — and no establishment senders exist until Phase 2) in docs/adr/
  - Landed early (2026-07-13, John's request alongside T162/T164): `docs/adr/0004-data-protection-key-custody.md` — records the known-plaintext key-ring posture T162 surfaced, the Azure Key Vault + IXmlEncryptor + secrets-port decision, and the "Production refuses to start without custody" gate now on T139. Not an R-numbered research decision like the rest of this backlog; recorded now because the decision needed to exist before T139 could be scoped, not deferred to T141's general sweep.
  - Landed early (2026-07-17, docs batch C1): `docs/adr/0006-solo-dev-review-gate.md` — records the constitution's "peer review required for all merges" clause amended to a gate solo-developer operation can actually satisfy (documented human review of AI-authored changes, or independent higher-tier AI review of human-authored changes, plus the full CI gate set; unreviewed direct merges still prohibited in either mode). Codifies the already-in-effect "Solo-dev merge model" practice rather than proposing new process. Generated constitution (`.specify/memory/constitution.md`) bumped 1.4.0 → 1.5.0 and its Code-quality bullet reconciled to match `docs/constitution-statement.md`'s wording verbatim.
- [ ] T142 [P] Documentation: OpenAPI review sign-off, module boundary map, quickstart.md validation run in docs/
- [ ] T143 Run all quickstart.md scenarios 1–12 end-to-end and record results in specs/001-specpour-v1/quickstart-results.md

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)** → **Foundational (Phase 2)** → all user stories
- **US1 (Phase 3)**: requires only Foundational — MVP
- **US2 (Phase 4)**: requires Foundational (identity core exists there; US2 completes it)
- **US3 (Phase 5)**: requires US2 (accounts) + US1 entities (catalog/ingredients)
- **US4 (Phase 6)**: requires US1 (ingredient hierarchy) + US2 (accounts); T068 (AI foundation) also serves US12
- **US5 (Phase 7)**: requires US1 (recipes) + US2; house-made costing depends on US3
- **US16 (Phase 8)**: requires US2 + Foundational authorization; unblocks content workstream (T136)
- **US6 (Phase 9)**: requires US3 (authoring) + US1 (browse)
- **US7/US8 (Phases 10–11)**: require US1 entities only
- **US9 (Phase 12)**: requires US4 (inventory) + US5 (batch math); the menu-sourced path additionally requires US11 and the prep-sourced path requires US10 — acceptance tests for those two paths are tagged to run at the Phase 14 and Phase 13 checkpoints respectively (recipe-sourced path fully testable at Phase 12)
- **US10 (Phase 13)**: requires US3 (house-made) + US5 (batching) + US4 (inventory)
- **US11 (Phase 14)**: requires US5 (costing); print of prep/shopping artifacts depends on US9/US10
- **US12 (Phase 15)**: requires T068 + content stories for grounding
- **US13 (Phase 16)**: requires US1 + US2
- **US14 (Phase 17)**: requires US1 (recipes) — client-only; prep-list bar mode (FR-051) additionally requires US10, already satisfied by phase order (17 after 13)
- **US15 (Phase 18)**: requires US1 content + US3 (user-authored edits for conflicts) + US11 (pins)
- **Polish (Phase 19)**: after desired stories complete

### Parallel Opportunities

- All [P] tasks within a phase (different files/modules)
- After Phase 2: US1 and US2 teams in parallel; then US3/US4/US5/US16 largely in parallel; US7/US8/US13/US14 are low-dependency fillers any time after their prerequisites
- Acceptance-test tasks ([P], first in every story) can be written while the previous story is finishing — criteria agreed before implementation per Principle I

## Parallel Example: User Story 1

```bash
# Failing-first tests together:
Task: T030 backend/tests/Acceptance/Features/US01_DiscoverRecipes.feature
Task: T031 frontend/app/integration_test/us01_discover_test.dart
# Then module domains in parallel:
Task: T032 Catalog domain    Task: T033 Ingredients domain
Task: T034 Equipment domain  Task: T035 Glossary domain
```

## Implementation Strategy

### MVP First

1. Phases 1–2 (Setup + Foundational)
2. Phase 3 (US1) → **STOP and VALIDATE**: guest discovery demo (quickstart scenario 1)
3. Phase 4 (US2) → accounts demo → this pair is the deployable MVP

### Incremental Delivery

Each subsequent phase is an independently testable increment ending in a checkpoint;
deliver in phase order, validating the story's Independent Test at each checkpoint.
Recommended release slices: [US1+US2] → [US3+US4+US5+US16] → [US6–US11] → [US12–US15].

## Notes

- Every story starts red: verify acceptance tests fail before implementing
- [P] = different files, no incomplete-task dependencies
- Commit after each task or logical group; no merge without all four suites green
- OpenAPI paths are authored/reviewed (T007 + per-story path tasks) before endpoint implementation — contract-first

### Launch content checklist (not code — curator/counsel deliverables gated before public launch)

These are content authoring/legal-review deliverables, not build tasks — tracked here so they aren't lost, but they are done by curators/counsel, not in code:

- **FR-070 — responsible-service/consumption encyclopedia content**: professional responsible-beverage-service articles (service limits, recognizing intoxication, host duties) are REQUIRED launch content (flagged out of T150, which built only the messaging/support-resources code). The Glossary/Article schema (T035) and reader (Phase 10 T098) hold it; the articles themselves must be authored + counsel-reviewed before launch.
- **FR-067 per-jurisdiction message text + FR-069 per-jurisdiction support resources**: T150 seeded only the "default" strictest-rule fallback rows with generic copy. Real per-launch-market, counsel-reviewed message text and localized helpline/organization links must be entered (via Seeder/admin console) before launching in each market.
- Canonical repertoire, 300+ glossary terms, ingredient/equipment reference data (SC-... / plan): curated content packs beyond T040's test/dev subset.
